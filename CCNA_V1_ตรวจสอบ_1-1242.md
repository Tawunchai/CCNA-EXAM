# CCNA 200-301 — Version 1 (ข้อ 1–1242) : รายงานผลการตรวจสอบและแก้ไข

> ตรวจทานทั้ง **1,242 ข้อ** ใน `src/data/questions.ts` ทีละข้อ
> เกณฑ์ = หลัก CCNA 200-301 ที่ถูกต้องจริง (ไม่ยึดตามเฉลยในเอกสารต้นฉบับ)
> ทุกข้อที่แก้ มี `⚠️ หมายเหตุ (แก้เฉลย)` หรือ `⚠️ หมายเหตุ (แก้โจทย์)` กำกับอยู่ในคำอธิบายของแอปแล้ว

---

## สรุปผล

| รายการ | จำนวน |
|:---|---:|
| ข้อทั้งหมดที่ตรวจ | 1,242 |
| **เฉลยผิด → แก้แล้ว** | **9** |
| **โจทย์/ตัวเลือกผิด → แก้แล้ว** | **6** |
| คำอธิบายขัดกับเฉลย → แก้แล้ว | 1 |
| ผ่านการตรวจ ไม่ต้องแก้ | 1,226 |

> หมายเหตุ: ตัวเลข "เฉลยผิด" ในรอบนี้น้อย เพราะรอบก่อนหน้าได้แก้ไปแล้ว 116 ข้อ (ดู `CCNA_เฉลยผิด_ต้องแก้.md`) รอบนี้คือการไล่ตรวจซ้ำทั้งชุดเพื่อหาที่ยังหลงเหลือ

---

## 1. ข้อที่ **เฉลยผิด** (แก้คำตอบแล้ว)

| ข้อ | เดิมตอบ | **ที่ถูกต้อง** | หัวข้อ |
|----:|:---:|:---:|:---|
| 165 | C | **A** | PortFast |
| 436 | A | **D** | หน้าที่ของ Layer 3 switch |
| 487 | B | **D** | primary route ของคู่ floating static |
| 508 | C | **B** | อ่าน show interface หาสาเหตุลิงก์ช้า |
| 525 | สลับกลุ่ม | **(drag)** | AAA — Authentication / Authorization |
| 236 | สลับชิ้น | **(drag)** | หน้าที่ของ DHCP (พบตอนตรวจ V2) |
| 655 | D | **A** | host route /32 ต้องใช้ AD 115 (พบตอนตรวจ V2) |
| 750 | A,E | **A,B** | /29 ครอบไม่ถึง PC5 ที่เป็น .10 (พบตอนตรวจ V2) |
| 1026 | C | **D** | เหมือนข้อ 655 (พบตอนตรวจ V2) |

---

## 2. ข้อที่ **โจทย์หรือตัวเลือกผิด** (แก้ข้อความแล้ว)

| ข้อ | ปัญหาที่พบ | สิ่งที่แก้ |
|----:|:---|:---|
| 34 | โจทย์ถูกตัดจนกำกวม (“Which IP address is the source IP?”) | เติมเงื่อนไข “หลัง CEF switch ออกสู่ outside” |
| 85 | ตัวเลือก B และ D ใช้ `ip access-group` ใต้ `line vty` — ไม่มีคำสั่งนี้จริงใน IOS | เปลี่ยนเป็น `access-class 100 in` |
| 234 | drag วาง `line vty 0 4` ไว้ช่องที่ 4 | เปลี่ยนเป็น `exit` (ตรงกับข้อ 1223 ที่เป็นโจทย์เดียวกัน) |
| 448 | ตัวเลือก B ใช้ชื่ออินเทอร์เฟซ `gi0` ที่ไม่มีอยู่ใน exhibit | แก้เป็น `gi0/1` |
| 606 | ตัวเลือก C ถูกแต่งขึ้นเอง (มี floating route เกินมา) ทำให้มี 2 ข้อที่ตอบถูก | คืนชุดตัวเลือกจริงตามข้อสอบ (เฉลยยังเป็น C) |
| 818 | มีตัวเลือกมาแค่ 3 ข้อ (A–C) | เติมข้อ D เป็นตัวลวง mask ผิด (เฉลยยังเป็น C) |
| 1092 | ทุกตัวเลือกใช้ mask 255.255.252.0 (/22) ทั้งที่ exhibit เป็น /23 และข้อ B มีคำว่า `native` เกินมา | แก้ mask เป็น 255.255.254.0 และตัด `native` ออก (เฉลยยังเป็น B) |

---

## 3. ข้อที่ **คำอธิบายขัดกับเฉลย**

| ข้อ | ปัญหา | สิ่งที่แก้ |
|----:|:---|:---|
| 29 | เฉลย A ถูกแล้ว แต่เนื้อคำอธิบายยังเชียร์ C อยู่ | เขียนคำอธิบายใหม่ให้สอดคล้องกับ A |

---

## 4. ข้อที่ต้องรู้ไว้ (ไม่ได้แก้ แต่มีข้อสังเกต)

| ข้อ | ข้อสังเกต |
|----:|:---|
| 440 | ตามหลัก EUI-64 เป๊ะ ๆ ต้องพลิกบิต U/L ทำให้ C6 → C4 แต่ตัวเลือกไม่มี C4 ให้เลือก → A เป็นคำตอบที่ใกล้ที่สุด (มีหมายเหตุในแอปแล้ว) |
| 704 | เป็นคำถามระบบ ERP/SAP (MRP low-level code) ไม่ใช่เนื้อหา CCNA เลย — หลุดปนมาจาก dump (เฉลย C ถูกต้องตามหลัก SAP) |
| 448 | เป็นโจทย์ที่ต้นฉบับออกแบบไม่รัดกุม (host route /32 ชนะด้วย longest prefix ไม่เกี่ยวกับ AD) แต่เฉลย D ตรงกับเฉลยที่ใช้กันทั่วไป |
| 716 | ข้อความในตัวเลือก C เขียนว่า “priority is highest” ซึ่งกลับกับหลัก STP แต่ SW4 เป็นคำตอบที่ถูก (มีคำอธิบายกำกับแล้ว) |

---

## 5. การตรวจอัตโนมัติ (test case)

สคริปต์ตรวจโครงสร้างทั้งไฟล์ ผลลัพธ์ **ผ่านทุกข้อ**:

| การตรวจ | ผล |
|:---|:---|
| id ครบ 1–1242 ไม่ซ้ำ ไม่ขาด | ✅ 0 ปัญหา |
| ทุก key ใน `correct` มีอยู่จริงในตัวเลือก | ✅ 0 ปัญหา |
| ลำดับตัวเลือกเป็น A→B→C→D ต่อเนื่อง | ✅ 0 ปัญหา |
| `single` ต้องมี 1 คำตอบ / `multi` ต้องมี ≥2 | ✅ 0 ปัญหา |
| จำนวนคำตอบตรงกับ “Choose two/three” ในโจทย์ | ✅ 0 ปัญหา |
| ทุกข้อมีตัวเลือกครบ 4 ข้อขึ้นไป | ✅ 0 ปัญหา |
| ตัวอักษรใน `✅ ทำไม X ถูก` ตรงกับเฉลยจริง | ✅ 0 ปัญหา |
| ไม่มีข้อไหนที่ `❌ ทำไมข้ออื่นผิด` ระบุตัวเลือกที่เป็นคำตอบ | ✅ 0 ปัญหา |
| drag: ไม่มี item ซ้ำ / ไม่มีหมวดว่าง | ✅ 0 ปัญหา |
| โจทย์ซ้ำกัน 37 กลุ่ม — เฉลยตรงกันทุกกลุ่ม | ✅ 0 ขัดแย้ง |
| TypeScript typecheck + build | ✅ ผ่าน |

---

*อัปเดตล่าสุด: 5 สิงหาคม 2026*
