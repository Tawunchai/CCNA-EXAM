# CCNA 200-301 — รายการข้อที่เฉลยต้นฉบับผิดตามหลัก CCNA

> ตรวจทานตามเนื้อหา CCNA 200-301 และเทียบกับ 9tut / ExamTopics / itexamanswers / Cisco docs  
> ใช้แก้เฉลยในเอกสารต้นฉบับให้ถูกต้อง — ทุกข้อในแอปมี ⚠️ หมายเหตุ กำกับพร้อมคำอธิบายเต็ม

## รวม **116 ข้อ** ที่เฉลยเอกสารผิด (จาก 700 ข้อ ≈ 17%)

| ข้อ | เอกสารตอบ | ที่ถูก | สรุปเหตุผล |
|----:|:---:|:---:|:---|
| 103 | AC | **C,E** | เฉลยต้นฉบับตอบ A,C แต่ "asynchronous routing" ไม่ใช่คำศัพท์ที่มีอยู่จริงในเครือข่าย (เป็นตัวลวงล้วน ๆ) — คำตอบที่ถูกต้องคือ C,E |
| 110 | AE | **C,E** | เฉลยต้นฉบับตอบ A,E — แต่ข้อ A ("จุดศูนย์กลางสำหรับ association และ authentication server") เป็นหน้าที่ของ Wireless LAN Controller ไม่ใช่ L2 switch → คำตอบที่ถูกต้องคือ C,E |
| 128 | B | **C** | เฉลยต้นฉบับตอบ B — แต่ "fair queuing" เป็นเพียง "กลไก/วิธีจัดคิว" (เช่น WFQ) ไม่ใช่ "วัตถุประสงค์" ของ shaping → คำตอบที่ถูกต้องตามหลัก CCNA คือ C |
| 148 | AC | **C,E** | เฉลยต้นฉบับตอบ A,C — แต่ "asynchronous routing" ไม่ใช่คำศัพท์ที่มีอยู่จริงในเครือข่ายเลย (เป็นตัวลวงล้วน ๆ) → คำตอบที่ถูกต้องคือ C,E |
| 150 | B | **D** | เฉลยต้นฉบับตอบ B (wireless access point) — ซึ่งจะถูกก็ต่อเมื่อเป็น autonomous AP (AP ที่ทำงานเองไม่มี WLC) แต่โจทย์ระบุชัดว่าเป็น "lightweight access point" → คำตอบที่ถูกต้องคือ D |
| 162 | D | **B** | เฉลยต้นฉบับตอบ D (ใช้ JSON library parse) — ผิดสองชั้น: (1) NETCONF ไม่ใช้ JSON เลย (2) การ parse ที่ฝั่ง client เกิดขึ้น "หลัง" ข้อมูลถูกส่งมาครบแล้ว → ไม่ได้ลดปริมาณที่เซิร์ฟเวอร์ส่งเลยแม้แต่ไบต์เดียว → คำตอบที่ถูกต้องคือ B |
| 163 | C | **A** | เฉลยต้นฉบับตอบ C (VM configuration file) — แต่ไฟล์ config (เช่น .vmx) เป็นของ แต่ละ VM แยกกันคนละไฟล์ ไม่ได้ใช้ร่วมกัน → คำตอบที่ถูกต้องคือ A |
| 182 | D | **A** | เฉลยต้นฉบับตอบ D — แต่ "no cdp run" เป็นคำสั่ง global ที่ปิด CDP ทั้งอุปกรณ์ (พิมพ์ในโหมด interface ก็ไม่มีผลตามที่ต้องการ) ซึ่งเกินกว่าที่โจทย์ขอมาก → คำตอบที่ถูกต้องคือ A |
| 184 | BE | **A,D** | เฉลยต้นฉบับตอบ B,E — ทั้งคู่ผิด: |
| 186 | B | **D** | เฉลยต้นฉบับตอบ B ("เซิร์ฟเวอร์จริงต้องรัน OS ทีละระบบ") — ผิดชัดเจน เพราะนั่นคือลักษณะของยุค ก่อน มี virtualization! จุดขายทั้งหมดของ VM คือการรัน OS หลายระบบพร้อมกัน → คำตอบที่ถูกต้องคือ D |
| 191 | D | **A** | เฉลยต้นฉบับตอบ D — แต่ D ขึ้นต้นด้วย `no cdp run` ซึ่ง ปิด CDP ทั้งเครื่อง ทำให้ `cdp enable` ที่ตามมาไม่มีผลใด ๆ → ผลลัพธ์คือ ไม่ส่ง CDP ให้ใครเลย รวมทั้ง Router C ด้วย → คำตอบที่ถูกต้องคือ A |
| 192 | B | **D** | เฉลยต้นฉบับตอบ B ("enable password-encryption") — ไม่มีคำสั่งนี้อยู่จริงใน IOS เป็นคำที่แต่งขึ้นมาหลอก → คำตอบที่ถูกต้องคือ D |
| 193 | B | **A** | เฉลยต้นฉบับตอบ B ("เชื่อมต่อโดยตรงระหว่าง subscriber") — ผิดตรงข้ามเลย! hub-and-spoke ไม่มีการเชื่อมตรงระหว่าง spoke การเชื่อมตรงทุกคู่คือลักษณะของ full mesh ต่างหาก → คำตอบที่ถูกต้องคือ A |
| 198 | BC | **A,B** | เฉลยต้นฉบับตอบ B,C — แต่ C ("continuous, redundant uptime") เป็นคำบรรยายของ core layer ชัดเจน (core คือชั้นที่ต้องไม่ล่มเด็ดขาด) → คำตอบที่ถูกต้องคือ A,B |
| 201 | A | **B** | เฉลยต้นฉบับตอบ A ("เข้ากันได้กับ 802.11b/g") — ผิดชัดเจน เพราะ 802.11a อยู่ 5 GHz ส่วน b/g อยู่ 2.4 GHz คนละย่านความถี่ อุปกรณ์คุยกันไม่ได้ → คำตอบที่ถูกต้องคือ B |
| 217 | CD | **B,C** | เฉลยต้นฉบับตอบ C,D — แต่ "model" ไม่ใช่องค์ประกอบของ Ansible เลย (YANG model เป็นเรื่องของ NETCONF/RESTCONF คนละเครื่องมือ) → คำตอบที่ถูกต้องคือ B,C |
| 218 | สลับกลุ่ม | **(drag)** | เฉลยต้นฉบับ สลับกลุ่มผิด — "distributed control plane" ถูกจัดไปอยู่ฝั่ง Controller-Based ซึ่งขัดแย้งกับนิยามของ SDN โดยตรง (SDN = รวมศูนย์ ไม่ใช่กระจาย!) → ที่ถูกต้องคือมันเป็นของ Traditional |
| 219 | AB | **A,D** | เฉลยต้นฉบับตอบ A,B — แต่ B บอกว่าเข้าสู่ Unmanaged state ซึ่งผิด อุปกรณ์ที่เพิ่มสำเร็จจะเข้าสู่ Managed state (ไม่งั้นจะเพิ่มเข้ามาทำไม?) → คำตอบที่ถูกต้องคือ A,D |
| 221 | DE | **B,D** | เฉลยต้นฉบับตอบ D,E — แต่ E คือ `ipv6 route 2001:db8:23::14/128 fd00:12::2 200` ซึ่งชี้ host ::14 ไปที่ R2 ไม่ใช่ R3! ขัดกับข้อกำหนดข้อที่ 2 โดยตรง (แถมยังใส่ AD 200 ทำให้เป็น floating static ที่ไม่ถูกใช้ด้วยซ้ำ) → คำตอบที่ถูกต้องคือ B,D |
| 226 | C | **B** | เฉลยต้นฉบับตอบ C ("แยกแยะทราฟฟิก voice กับ video") — แต่นั่นเป็นเพียง ขั้นตอนหนึ่ง (classification) ในกระบวนการ ไม่ใช่ ผลลัพธ์ ที่ทำให้เสียงดีขึ้น โจทย์ถามว่า QoS "optimize voice" ยังไง → คำตอบต้องเป็นผลลัพธ์ = ลด packet loss → คำตอบที่ถูกต้องคือ B |
| 229 | A | **C** | เฉลยต้นฉบับตอบ A ("เข้าถึงได้ทางไร้สายเท่านั้น") — ผิดชัดเจน คลาวด์เข้าถึงได้ทั้งทาง มีสาย, ไร้สาย, VPN, MPLS — ไม่มีข้อจำกัดว่าต้องไร้สาย → คำตอบที่ถูกต้องคือ C |
| 232 | สลับกลุ่ม | **(drag)** | เฉลยต้นฉบับ สลับสองกลุ่มผิด — เอา "FC00::/7" และ "exclusive internal use" ไปไว้ฝั่ง Link-Local ซึ่งผิด (Link-Local คือ FE80::/10 ต่างหาก) → ที่ถูกต้องคือทั้งสองข้อความนั้นเป็นของ Unique Local |
| 242 | B | **C** | เฉลยต้นฉบับตอบ B — แต่ผิด สองชั้น: |
| 251 | C | **D** | เฉลยต้นฉบับตอบ C ซึ่งสับสนระหว่าง Facility กับ Severity — ตามนิยาม syslog (RFC 5424) และเนื้อหา CCNA คำตอบที่ถูกต้องคือ D |
| 252 | CE | **A,C** | เฉลยต้นฉบับตอบ C,E แต่ E คือ hybrid cloud ไม่ใช่ public — คำตอบที่ถูกต้องคือ A,C |
| 271 | C | **B** | เฉลยต้นฉบับตอบ C (dynamic auto) ซึ่งผิด เพราะ auto เป็นโหมดเชิงรับ — คำตอบที่ถูกต้องคือ B |
| 272 | .254 | **C** | เฉลยต้นฉบับตอบ .254 ซึ่งผิด เพราะ 172.16.0.14 ไม่อยู่ในช่วง 172.16.3.0/28 เลย — คำตอบที่ถูกต้องคือใช้ default route (.246) |
| 279 | A | **B** | เฉลยต้นฉบับตอบ A ซึ่งผิด เพราะ DHCPv6 ไม่ได้สร้าง address จาก MAC — คำตอบที่ถูกต้องคือ B |
| 292 | BC | **A,B** | เฉลยต้นฉบับตอบ B,C แต่ .64 อยู่นอกช่วง /26 นี้อย่างชัดเจน (subnet นี้จบที่ .63) — คำตอบที่ถูกต้องคือ A,B |
| 301 | A | **B** | เฉลยต้นฉบับตอบ A (255.255.0.0) แต่ผิด — /16 ให้ถึง 65,534 hosts เปลืองสุด ๆ ไม่ "efficient" เลย คำตอบที่ถูกต้องคือ B |
| 305 | C | **D** | เฉลยต้นฉบับตอบ C (NetFlow) แต่ผิด — คำถามเน้น "highest security" ซึ่ง NetFlow ไม่มีระบบความปลอดภัยในตัว คำตอบที่ถูกต้องคือ D (SNMPv3) |
| 337 | A | **B** | เฉลยต้นฉบับตอบ A แต่ผิด — การ routing ต้อง "match ช่วงจริง" ไม่ใช่ "คล้าย ๆ" คำตอบที่ถูกต้องคือ B (ทิ้งแพ็กเก็ต) |
| 372 | C | **B** | เฉลยต้นฉบับตอบ C (ec keysize 1024) แต่ผิด — EC (elliptic curve) บน Cisco IOS รองรับ keysize เพียง 256/384/521 เท่านั้น "1024 และ 2048 เป็นค่าที่ไม่ถูกต้องสำหรับ EC" คำสั่งจะถูกปฏิเสธ คำตอบที่ถูกต้องคือ B |
| 380 | D | **B** | เฉลยต้นฉบับตอบ D แต่ผิด — eBGP มี AD ต่ำกว่า OSPF จึง "แทนที่" เส้นเดิม คำตอบที่ถูกต้องคือ B |
| 384 | C | **D** | เฉลยต้นฉบับตอบ C แต่ผิด — 802.1x ไม่เกี่ยวกับการกัน spoofed association/Comeback timer คำตอบที่ถูกต้องคือ D |
| 391 | A | **D** | เฉลยต้นฉบับตอบ A แต่ผิด — Cisco ใช้ RRM/DCA วิเคราะห์และเลือกช่องอัตโนมัติ คำตอบที่ถูกต้องคือ D |
| 393 | C | **B** | เฉลยต้นฉบับตอบ C แต่ผิด — C ใช้ "ip nat outside destination" (แปลงปลายทาง) ไม่ใช่แปลง "source" ตามโจทย์ คำตอบที่ถูกต้องคือ B |
| 401 | A | **D** | เฉลยต้นฉบับตอบ A แต่ผิด — การกระจาย VLAN คือ VTP ไม่ใช่ DHCP snooping คำตอบที่ถูกต้องคือ D |
| 410 | B | **D** | เฉลยต้นฉบับตอบ B แต่ผิด — สวิตช์ flood เมื่อ "ปลายทาง" ไม่รู้จัก ไม่ใช่ "ต้นทาง" คำตอบที่ถูกต้องคือ D |
| 411 | B | **C** | เฉลยต้นฉบับตอบ B แต่คำสั่งมาตรฐานที่ทำให้เป็น server อ้างอิงนาฬิกาภายในคือ "ntp master" คำตอบที่ถูกต้องคือ C |
| 415 | AB | **B,C** | เฉลยต้นฉบับตอบ A,B แต่ A ผิด (PortFast "ข้าม" listening ไม่ใช่เข้า listening) คำตอบที่ถูกต้องคือ B,C |
| 416 | BE | **A,D** | เฉลยต้นฉบับตอบ B,E แต่ผิด — B คือ BSSID (ไม่ใช่ SSID) และ SSID ไม่ได้ให้ความปลอดภัย คำตอบที่ถูกต้องคือ A,D |
| 427 | D | **A** | เฉลยต้นฉบับตอบ D แต่เราไปตั้งค่าอุปกรณ์ของ ISP (third-party) ไม่ได้ คำตอบที่ถูกต้องคือ A |
| 438 | A | **D** | เฉลยต้นฉบับตอบ A (10.10.10.5) แต่ผิดจากการคำนวณช่วง — คำตอบที่ถูกต้องคือ D (10.10.10.9) |
| 449 | สลับกลุ่ม | **(drag)** | เฉลยต้นฉบับสลับข้างผิด (จับ "distributed control plane" ไปฝั่ง controller-based และ "centralized" ไปฝั่ง traditional) — ที่ถูกต้องเป็นดังนี้ |
| 451 | A | **B** | เฉลยต้นฉบับตอบ A (broadcast มากเกิน) แต่ผิด — มี broadcast แค่ 67 (น้อยมาก) ตัวชี้ปัญหาจริงคือ CRC error จำนวนมหาศาล คำตอบที่ถูกต้องคือ B |
| 453 | B | **D** | เฉลยต้นฉบับตอบ B (static NAT) แต่ไม่เข้ากับดีไซน์ PAT overload ที่มีอยู่ คำตอบที่ถูกต้องคือ D |
| 459 | C | **A** | เฉลยต้นฉบับตอบ C (SW3) แต่ผิด — SW1 มี priority ต่ำสุด คำตอบที่ถูกต้องคือ A |
| 462 | B | **C** | เฉลยต้นฉบับตอบ B แต่ผิด — "enable secret privilege 15 ..." เป็น syntax ที่ไม่ถูกต้อง คำตอบที่ถูกต้องคือ C |
| 463 | A | **B** | เฉลยต้นฉบับตอบ A แต่ผิด — คำตอบที่ถูกต้องคือ B |
| 469 | BC | **C,E** | เฉลยต้นฉบับตอบ B,C แต่ผิด — "RSA token" ไม่ใช่องค์ประกอบของ PKI คำตอบที่ถูกต้องคือ C,E |
| 476 | B | **A** | เฉลยต้นฉบับตอบ B แต่ผิด — "ntp stratum 2" ไม่ใช่คำสั่งที่ถูกต้อง (ต้องใช้ "ntp master 2") และ access-list 10 เป็น standard ACL จะใส่ "udp/port" ไม่ได้ คำตอบที่ถูกต้องคือ A |
| 479 | BD | **A,D** | เฉลยต้นฉบับตอบ B,D แต่ผิด — B (NETCONF config) เป็น management plane คำตอบที่ถูกต้องคือ A,D |
| 481 | B | **D** | เฉลยต้นฉบับตอบ B แต่ผิดจากการคำนวณช่วง — คำตอบที่ถูกต้องคือ D |
| 489 | A | **D** | เฉลยต้นฉบับตอบ A (รหัส console) แต่ผิด — รหัสผ่านเป็น "logical access control" ไม่ใช่ physical คำตอบที่ถูกต้องคือ D |
| 490 | สลับกลุ่ม | **(drag)** | เฉลยต้นฉบับสลับข้อความบางส่วน — ที่ถูกต้องตามหลัก CCNA เป็นดังนี้ |
| 491 | D | **B** | เฉลยต้นฉบับตอบ D แต่ผิด — "switchport trunk allowed vlan 104" จะ "แทนที่" รายการเดิมทั้งหมดให้เหลือแค่ 104 คำตอบที่ถูกต้องคือ B |
| 497 | BD | **B,E** | เฉลยต้นฉบับตอบ B,D แต่ D ผิด — 10.70.147.17 ไม่ใช่ "first usable" ของ /27 คำตอบที่ถูกต้องคือ B,E |
| 501 | B | **D** | เฉลยต้นฉบับตอบ B (fc00:...) แต่ที่อยู่นั้นเขียนไม่ครบ 8 กลุ่ม (malformed) และช่วง fc00::/8 ยังไม่ถูกกำหนดใช้งาน คำตอบที่ถูกต้องคือ D |
| 506 | B | **A** | เฉลยต้นฉบับตอบ B (link-local) แต่ link-local ใช้ได้แค่ "ลิงก์เดียว" route ข้าม subnet ในองค์กรไม่ได้ คำตอบที่ถูกต้องคือ A |
| 507 | C | **B** | เฉลยต้นฉบับตอบ C (remark ที่ R1) แต่ผิดหลัก — QoS ควร classify/mark ให้ "ใกล้ต้นทางที่สุด" ไม่ใช่ที่ R1 (แกน) คำตอบที่ถูกต้องคือ B |
| 512 | B | **A** | เฉลยต้นฉบับตอบ B (secret 5 = Type 5/MD5) แต่ยังไม่ใช่ที่แข็งที่สุด คำตอบที่ถูกต้องคือ A |
| 516 | ใส่ | **(drag)** | เฉลยต้นฉบับใส่ "Frames ... are discarded" ซึ่งผิด — ในสถานะ forwarding เฟรมต้องถูก "ประมวลผล/ส่งต่อ" ไม่ใช่ทิ้ง |
| 518 | B | **A** | เฉลยต้นฉบับตอบ B (192.168.0.4) แต่ผิดจากหลัก Longest Prefix Match คำตอบที่ถูกต้องคือ A |
| 519 | D | **B** | เฉลยต้นฉบับตอบ D แต่ D มี "MTU mismatch" (R14=1500, R86=1400) ซึ่งทำให้ OSPF ค้างที่ EXSTART/EXCHANGE จับคู่ไม่สำเร็จ คำตอบที่ถูกต้องคือ B |
| 528 | C | **D** | เฉลยต้นฉบับตอบ C แต่ C ขาดการตั้ง hostname และใช้ "transport input all" (ยังเปิด Telnet) คำตอบที่ครบถ้วนถูกต้องตาม CCNA คือ D |
| 531 | B | **A** | เฉลยต้นฉบับตอบ B แต่ B ใช้ "tcp" — DHCP วิ่งบน UDP ไม่ใช่ TCP คำตอบที่ถูกต้องคือ A |
| 533 | C | **A** | เฉลยต้นฉบับตอบ C แต่ C ใช้ subnet ใหญ่เกินจำเป็น (เปลืองมาก) คำตอบที่ถูกต้อง (พอดี + 10%) คือ A |
| 534 | C | **A** | เฉลยต้นฉบับตอบ C (WEP) ซึ่งผิดอย่างชัดเจน — WEP เป็นการเข้ารหัสที่เก่าและแตกง่ายที่สุด ไม่มี forward secrecy เลย คำตอบที่ถูกต้องคือ A |
| 540 | AE | **D,E** | เฉลยต้นฉบับตอบ A,E แต่ A ใช้ modulus 1024 ซึ่งไม่ได้ "เพิ่มเป็นสองเท่า" ตามโจทย์ คำตอบที่ถูกต้องคือ D,E |
| 543 | สลับกลุ่ม | **(drag)** | เฉลยต้นฉบับสลับข้อความ Anycast กับ Multicast บางส่วน — ที่ถูกต้องตามหลัก CCNA เป็นดังนี้ |
| 545 | D | **C** | เฉลยต้นฉบับตอบ D (10.165.20.226) แต่ผิดจากหลัก Longest Prefix Match คำตอบที่ถูกต้องคือ C |
| 547 | A | **B** | เฉลยต้นฉบับตอบ A แต่ A พูดถึง "Cisco devices" ซึ่งขัดกับโจทย์ multivendor คำตอบที่ถูกต้องคือ B |
| 549 | C | **A** | เฉลยต้นฉบับตอบ C แต่ C "ขาด ip domain-name" ทำให้สร้าง RSA key ไม่ได้ (SSH ใช้งานไม่ได้) คำตอบที่ถูกต้องคือ A |

| 553 | C,E | **B,E** | เฉลยต้นฉบับตอบ C,E แต่ "cryptographic keychain" เป็นกลไกยืนยันตัวตนของ routing protocol (เช่น EIGRP/OSPF authentication) ไม่ใช่วิธี login เข้าอุปกรณ์ และไม่ใช่แนวปฏิบัติพื้นฐานของ security posture — คำตอบที่ถูกต้องคือ B,E |
| 555 | C | **A** | เฉลยต้นฉบับตอบ C แต่ C ผิด 2 ชั้น (ใช้คำสั่ง "ip route" ซึ่งเป็นของ IPv4 และพิมพ์ในโหมด config-if) คำตอบที่ถูกต้องคือ A |
| 562 | D | **A** | เฉลยต้นฉบับตอบ D (4) โดยนับ "จำนวนสวิตช์" แต่ตามนิยาม JSON จริง ๆ object = สิ่งที่ครอบด้วย { } เท่านั้น ในโค้ดนี้มีวงเล็บปีกกาแค่คู่เดียว → คำตอบที่ถูกต้องคือ A (1) |
| 567 | B | **A** | เฉลยต้นฉบับตอบ B แต่ B เขียน prefix เป็น ":: " โดยไม่มี prefix length (/0) ซึ่ง IOS ไม่รับ — คำตอบที่ถูกต้องคือ A |
| 568 | D | **B** | เฉลยต้นฉบับตอบ D (setting packet-handling policies) — แต่นั่นเป็นหน้าที่ของ "คอนโทรลเลอร์" ไม่ใช่สวิตช์ คำตอบที่ถูกต้องคือ B |
| 571 | B | **C** | เฉลยต้นฉบับตอบ B (192.168.7.7) ซึ่งเป็น next-hop ของ 172.16.1.0/28 — แต่ /28 ครอบแค่ .0–.15 ไม่ครอบ .190 คำตอบที่ถูกต้องคือ C |
| 573 | A | **C** | เฉลยต้นฉบับตอบ A (802.1x authentication) — แต่ 802.1X ไม่ใช่ของใหม่ที่ WPA นำมา (มีใช้กับ dynamic WEP มาก่อน) และมีทั้งใน WPA/WPA2/WPA3 คำตอบที่ถูกต้องคือ C |
| 578 | A | **A,C** | เฉลยต้นฉบับให้มาแค่ A ทั้งที่โจทย์บอก "Choose two" — คำตอบที่ครบถูกต้องคือ A และ C |
| 585 | C | **B** | เฉลยต้นฉบับตอบ C (DHCP address assignment) — แต่การบังคับ "DHCP Addr. Assignment Required" จะ "ปฏิเสธ" ไคลเอนต์ที่ตั้ง IP เองด้วยซ้ำ ตรงข้ามกับโจทย์ คำตอบที่ถูกต้องคือ B |
| 597 | C | **A** | เฉลยต้นฉบับตอบ C (เครือข่ายใหญ่หลายไซต์) — ซึ่งตรงข้ามกับหลักการ เพราะเครือข่ายใหญ่ต้องใช้ 3-tier เต็มรูปแบบ คำตอบที่ถูกต้องคือ A |
| 599 | B,D | **A,B** | เฉลยต้นฉบับตอบ B,D แต่ WEP ใช้ RC4 ไม่ใช่ CCMP เด็ดขาด — คำตอบที่ถูกต้องคือ A,B |
| 600 | A | **C** | เฉลยต้นฉบับตอบ A ("ทำให้เกิด network loop") ซึ่งไม่จริง — port security ไม่เคยสร้างลูป คำตอบที่ถูกต้องคือ C |
| 607 | B | **D** | เฉลยต้นฉบับตอบ B (snmp-server community) — แต่ community string เป็นกลไกของ SNMPv1/v2c ต่างหาก SNMPv3 ไม่ใช้ community เลย คำตอบที่ถูกต้องคือ D |
| 612 | B | **D** | เฉลยต้นฉบับตอบ B (four) โดยนับเฉพาะ object ที่ "มีชื่อ" (aaaUser, aaaUserDomain, aaaUserRole + วงนอกสุด) — แต่ตามนิยาม JSON ทุกคู่ { } คือ object คำตอบที่ถูกต้องคือ D (nine) |
| 616 | A | **B** | เฉลยต้นฉบับตอบ A — แต่ 203.0.113.1 เป็น next hop ของ "static route" เอง การที่มันยังถึงได้เป็นเพียงเงื่อนไขให้ static route "ใช้ได้" ไม่ใช่เหตุที่ทำให้ถูก "ติดตั้ง" ลงตาราง คำตอบที่ถูกต้องคือ B |
| 618 | B | **D** | เฉลยต้นฉบับตอบ B (ตั้งรหัสผ่าน console) — แต่นั่นคือ "logical access control" (ควบคุมผ่านซอฟต์แวร์) ไม่ใช่ physical คำตอบที่ถูกต้องคือ D |
| 626 | B | **D** | เฉลยต้นฉบับตอบ B ("กำหนดความรุนแรงของข้อความ") — แต่ระดับ severity ถูกกำหนดโดย "ตัวเหตุการณ์" เองอยู่แล้ว เราตั้งค่าไม่ได้ สิ่งที่เราตั้งคือ "จะเก็บถึงระดับไหน" คำตอบที่ถูกต้องคือ D |
| 628 | B | **B,D** | เฉลยต้นฉบับให้มาแค่ B ทั้งที่โจทย์บอก "Choose two" — คำตอบที่ครบถูกต้องคือ B,D |
| 630 | B,E | **D,E** | เฉลยต้นฉบับตอบ B,E — แต่ B ผิด เพราะอินเทอร์เฟซหนึ่งใส่ "ip helper-address" ได้หลายบรรทัด (ชี้ไป DHCP server หลายตัวเพื่อความซ้ำซ้อน) คำตอบที่ถูกต้องคือ D,E |
| 634 | B | **C** | เฉลยต้นฉบับตอบ B — แต่ "config sessions timeout" คุม session ทาง "เครือข่าย" (Telnet/SSH) ส่วนโจทย์ระบุชัดว่าเป็น "serial session" (พอร์ตคอนโซล) คำตอบที่ถูกต้องคือ C |
| 635 | D | **A** | เฉลยต้นฉบับตอบ D (4) โดยนับจำนวนสวิตช์ — แต่ตามไวยากรณ์ JSON object คือสิ่งที่อยู่ในคู่ { } ซึ่งมีเพียงคู่เดียว คำตอบที่ถูกต้องคือ A (1) |
| 637 | C | **B** | เฉลยต้นฉบับตอบ C — ซึ่งเป็น "รายละเอียดที่ตามมา" (native VLAN ควรตรงกับ management VLAN ของ AP) แต่เงื่อนไขหลักที่ต้องมีก่อนคือพอร์ตต้องเป็น trunk คำตอบที่ถูกต้องคือ B |
| 639 | D | **B** | เฉลยต้นฉบับตอบ D (power policing) — แต่ power policing เป็นฟีเจอร์แยก (สั่งด้วย "power inline police") ไม่ได้เปิดอัตโนมัติเพราะเลือกโหมด auto คำตอบที่ถูกต้องคือ B |
| 640 | D | **C** | เฉลยต้นฉบับตอบ D (access) — แต่พอร์ต access ส่งได้ VLAN เดียวเท่านั้น ไม่สามารถแยก 2 VLAN ได้ คำตอบที่ถูกต้องคือ C |
| 646 | B | **A** | เฉลยต้นฉบับตอบ B (DNS Servers) — แต่ 8.8.8.8 คือ Google Public DNS ซึ่งเป็นรีโซลเวอร์สาธารณะที่ใช้งานได้จริง ไม่ใช่ค่าที่ผิด คำตอบที่ถูกต้องคือ A |
| 651 | A,B | **B,E** | เฉลยต้นฉบับตอบ A,B — แต่สายแพตช์เสียทำให้เกิด CRC error/ลิงก์ล้ม ไม่ได้ทำให้ "flood" คำตอบที่ถูกต้องคือ B,E |
| 654 | C | **A,B** | เฉลยต้นฉบับตอบ C ทั้งที่โจทย์ขอ "Choose two" และ C ยังเป็นการกำหนด MAC แบบ static ซึ่งขัดกับข้อกำหนด "learned dynamically" คำตอบที่ถูกต้องคือ A,B |
| 656 | A | **C** | เฉลยต้นฉบับตอบ A (excessive collisions) — แต่ในเอาต์พุตมีเพียง "11 collisions" ซึ่งน้อยมาก และพอร์ตทำงานแบบ full-duplex ที่ไม่ควรมี collision อยู่แล้ว คำตอบที่ถูกต้องคือ C |
| 661 | A | **D** | เฉลยต้นฉบับตอบ A — แต่ A ไม่ได้ระบุว่า MAC นี้อยู่บน voice VLAN จึงจะไปผูกกับ access VLAN แทน คำตอบที่ถูกต้องคือ D |
| 665 | B | **C** | เฉลยต้นฉบับตอบ B (software virtual interface) — แต่ SVI คืออินเทอร์เฟซ VLAN บนสวิตช์ ไม่เกี่ยวกับการสื่อสารระหว่างโปรแกรมเลย คำตอบที่ถูกต้องคือ C |
| 667 | D | **C** | เฉลยต้นฉบับตอบ D ("ใช้เส้นทางที่คล้ายกับปลายทาง") — แต่การเราต์ไม่มีคำว่า "คล้าย" ต้อง match ช่วงจริงเท่านั้น คำตอบที่ถูกต้องคือ C |
| 670 | D | **A** | เฉลยต้นฉบับตอบ D ([1/0] via 10.10.10.2) — แต่ static route ทั้งสามเส้นถูกกำหนด AD เอง (111, 112, 108) จึงไม่มีเส้นใดมี AD = 1 คำตอบที่ถูกต้องคือ A |
| 678 | A,D | **D,E** | เฉลยต้นฉบับตอบ A,D — แต่ "Get" มีมาตั้งแต่ SNMPv1 ไม่ใช่ของใหม่ใน v2 คำตอบที่ถูกต้องคือ D,E |
| 681 | A | **B** | เฉลยต้นฉบับตอบ A (FF00::/8) — แต่นั่นคือช่วง "multicast" ไม่ใช่ anycast คำตอบที่ถูกต้องคือ B |
| 682 | B | **D** | เฉลยต้นฉบับตอบ B — แต่ AP ตัวนี้เป็น autonomous AP ซึ่งไม่ได้อยู่ภายใต้การจัดการของ WLC อยู่แล้ว จึง "ถอดออกจาก WLC" ไม่ได้ คำตอบที่ถูกต้องคือ D |
| 683 | D | **A** | เฉลยต้นฉบับตอบ D (ลดความซับซ้อนของเครือข่าย) — ซึ่งเป็นเพียงผลพลอยได้ ไม่ใช่วัตถุประสงค์หลัก คำตอบที่ถูกต้องคือ A |
| 685 | D | **B** | เฉลยต้นฉบับตอบ D — แต่ out-of-band management บน WLC ใช้ "service port" ต่างหาก ไม่ใช่ distribution port คำตอบที่ถูกต้องคือ B |
| 686 | A | **D** | เฉลยต้นฉบับตอบ A (dynamic flow identification) — แต่การระบุ flow คือหน้าที่ของ classification/NBAR ไม่ใช่ shaping คำตอบที่ถูกต้องคือ D |
| 687 | B | **A** | เฉลยต้นฉบับตอบ B — แต่ในสถาปัตยกรรม collapsed core ชั้น distribution ไม่ได้แยกออกมาต่างหาก มันถูกยุบรวมเข้ากับ core แล้ว คำตอบที่ถูกต้องคือ A |
| 691 | D | **C** | เฉลยต้นฉบับตอบ D (administrative distance) — แต่ AD จะถูกใช้ก็ต่อเมื่อเส้นทาง "มี prefix เท่ากัน" เท่านั้น ในภาพทั้งสามเส้นมี prefix ต่างกัน (/27, /19, /24) จึงถูกติดตั้งในตารางพร้อมกันทั้งหมด และการ forward ตัดสินด้วย longest prefix คำตอบที่ถูกต้องคือ C |
| 693 | B | **D** | เฉลยต้นฉบับตอบ B ("ต้องมีตัวเลขและตัวอักษรอย่างละตัว") — ไม่มีข้อกำหนดเช่นนี้ในมาตรฐาน 802.11 เลย คำตอบที่ถูกต้องคือ D |
| 698 | C | **B** | เฉลยต้นฉบับตอบ C (access point) — ซึ่งเป็นอุปกรณ์กระจายสัญญาณไร้สาย ไม่ได้แบ่งโซนความปลอดภัย คำตอบที่ถูกต้องคือ B |

---

## เลขข้อทั้งหมด (คัดลอกไปกาในเอกสาร)
```
103, 110, 128, 148, 150, 162, 163, 182, 184, 186, 191, 192, 193, 198, 201, 217, 218, 219, 221, 226, 229, 232, 242, 251, 252, 271, 272, 279, 292, 301, 305, 337, 372, 380, 384, 391, 393, 401, 410, 411, 415, 416, 427, 438, 449, 451, 453, 459, 462, 463, 469, 476, 479, 481, 489, 490, 491, 497, 501, 506, 507, 512, 516, 518, 519, 528, 531, 533, 534, 540, 543, 545, 547, 549, 553, 555, 562, 567, 568, 571, 573, 578, 585, 597, 599, 600, 607, 612, 616, 618, 626, 628, 630, 634, 635, 637, 639, 640, 646, 651, 654, 656, 661, 665, 667, 670, 678, 681, 682, 683, 685, 686, 687, 691, 693, 698
```

## แยกตามช่วง
- **1–300**: 29 ข้อ — 103, 110, 128, 148, 150, 162, 163, 182, 184, 186, 191, 192, 193, 198, 201, 217, 218, 219, 221, 226, 229, 232, 242, 251, 252, 271, 272, 279, 292
- **301–350**: 3 ข้อ — 301, 305, 337
- **351–450**: 13 ข้อ — 372, 380, 384, 391, 393, 401, 410, 411, 415, 416, 427, 438, 449
- **451–500**: 13 ข้อ — 451, 453, 459, 462, 463, 469, 476, 479, 481, 489, 490, 491, 497
- **501–550**: 16 ข้อ — 501, 506, 507, 512, 516, 518, 519, 528, 531, 533, 534, 540, 543, 545, 547, 549
- **551–700**: 42 ข้อ — 553, 555, 562, 567, 568, 571, 573, 578, 585, 597, 599, 600, 607, 612, 616, 618, 626, 628, 630, 634, 635, 637, 639, 640, 646, 651, 654, 656, 661, 665, 667, 670, 678, 681, 682, 683, 685, 686, 687, 691, 693, 698

## หมายเหตุ (ไม่นับรวมข้างบน)
- **332, 345, 530**: ตัวเลือกในเอกสาร OCR เพี้ยน/พิมพ์ซ้ำจนอ่านไม่ได้ ผมสร้าง/เรียบเรียงตัวเลือกใหม่ตามหลัก CCNA (332→C, 345→D, 530→C) — เป็นกรณี "ตัวเลือกอ่านไม่ได้" ไม่ใช่ "เฉลยผิด"
- **606, 629, 645, 652, 694, 695, 697**: เอกสารต้นฉบับมีภาพตัวเลือก/ตารางเราต์ที่อ่านไม่ได้ หรือรูปถูกสลับกับข้ออื่น จึงเรียบเรียงโจทย์/ตัวเลือกใหม่ให้ทดสอบหลักการเดิม (มี 🔍 หมายเหตุกำกับในแอปทุกข้อ) — เป็นกรณี "โจทย์อ่านไม่ได้" ไม่ใช่ "เฉลยผิด"
- **572, 579, 581, 586, 595, 598, 608, 621, 638, 644, 649, 660, 673, 676, 679, 680**: เอกสารเขียนตัวเลือกไว้เป็น "Option A/B/C/D" โดยตัวคำสั่งจริงอยู่ในภาพที่ไม่ได้แนบมา จึงเขียนชุดคำสั่งขึ้นใหม่ให้ตรงกับ exhibit และหลัก CCNA (คำตอบที่ถูกยังตรงกับตัวอักษรเดิมของเอกสาร)
