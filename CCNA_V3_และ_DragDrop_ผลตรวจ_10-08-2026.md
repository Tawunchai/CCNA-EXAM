# ผลตรวจสอบเฉลย — Version 3 (317 ข้อ) + Drag-Drop Exam (249 ข้อ)

> วันที่ตรวจ: 10 สิงหาคม 2026 · เกณฑ์ = หลัก CCNA 200-301 ที่ถูกต้องจริง ไม่ยึดเฉลยใน dump
> ไฟล์ที่ตรวจ: `src/data/questionsV3.ts` · `src/data/dragBank.ts` (รวม drag จาก V1 + V2 + V3)

---

## สรุปผล

| รายการ | จำนวน | ผล |
|:---|---:|:---|
| V3 — ข้อเลือกตอบ (single/multi) | 268 | ✅ เฉลยถูกทุกข้อ |
| V3 — drag | 49 | ✅ เฉลยถูกทุกข้อ |
| **V3 รวม** | **317** | **✅ ไม่พบเฉลยผิด** |
| Drag Exam — V1 | 115 | ✅ เฉลยถูกทุกข้อ |
| Drag Exam — V2 | 85 | ✅ เฉลยถูกทุกข้อ |
| Drag Exam — V3 | 49 | ✅ เฉลยถูกทุกข้อ |
| **Drag Exam รวม** | **249** | **✅ ไม่พบเฉลยผิด** |

**🔴 ข้อที่ต้องแก้เฉลย = 0 ข้อ**
**🟡 ข้อที่ "เฉลยถูกตามข้อสอบ แต่ตัวโจทย์ต้นฉบับบกพร่อง/กำกวม" = 13 รายการ** (ดูส่วนที่ 2 — ไม่ต้องแก้เฉลย แต่ควรรู้ก่อนเข้าห้องสอบ)

---

## 1) วิธีตรวจ

1. **ดึงข้อมูลจริงจากไฟล์** ด้วยสคริปต์ (ไม่ได้อ่านจากสรุปเก่า) → V3 = 317 ข้อ, drag = 115 + 85 + 49 = 249 ข้อ ✔ ตรงกับจำนวนที่แจ้ง
2. **ไล่อ่านโจทย์ + ตัวเลือก + เฉลยทีละข้อครบทั้ง 317 + 249 ข้อ** เทียบกับหลัก CCNA
3. **เปิดภาพ exhibit จริง 55 ใบ** สำหรับข้อที่เฉลยขึ้นกับภาพล้วน ๆ (ตาราง routing, ตัวนับ interface, JSON, คอนฟิก, ตาราง OSPF timer, MAC สำหรับ EUI-64, รายการไอเทมของ drag) แล้วคำนวณเองใหม่ทั้งหมด
4. **สคริปต์ตรวจความขัดแย้งข้ามชุด** — โจทย์เดียวกันในคนละชุดต้องเฉลยตรงกัน
5. **สคริปต์ตรวจโครงสร้าง** — id ซ้ำ, key ไม่มีในตัวเลือก, "Choose two" แต่เฉลย 1 ตัว, drag ที่หมวดว่าง, ไอเทม drag ซ้ำ ฯลฯ

### ผลสคริปต์
* ความขัดแย้งข้ามชุด (ข้อเลือกตอบ): **0** — พบ 1 คู่ที่โจทย์เหมือนกันแต่ **exhibit คนละใบ** (V3#260 vs V3#261) เปิดดูแล้วทั้งคู่ถูกต้อง
* ความขัดแย้ง drag: **0** — ที่สคริปต์รายงาน 35 รายการเป็นแค่ "ชื่อหมวดเขียนต่างกัน" (เช่น `Step 1` vs `1`, `Ansible features` vs `คุณสมบัติของ Ansible`) การจับคู่เหมือนกันทุกตัว
* ปัญหาเชิงโครงสร้าง: **0**

### ตัวอย่าง exhibit ที่เปิดคำนวณเองแล้วยืนยันว่าเฉลยถูก

| ข้อ | สิ่งที่ตรวจ | ผล |
|:---|:---|:---|
| V3#11 | 10.1.2.126 → longest match = `S 10.1.2.112/28` | ✅ C (10.165.20.166) |
| V3#17 | static AD 111/112/**108** + OSPF 110 → AD ต่ำสุด 108 | ✅ D |
| V3#37 | นับปีกกา `{` ใน JSON = 9 | ✅ D |
| V3#54 / #155 | 172.16.4.0 เป็น **/23** ใน exhibit หนึ่ง และ **/21** ในอีก exhibit หนึ่ง | ✅ ทั้งคู่ (ไม่ใช่ข้อขัดแย้ง) |
| V3#72 | R1/R2/R3 มีบรรทัด `ntp server` (R4 มีแต่ `ntp master`) | ✅ A |
| V3#85 / #91 / #113 / #143 / #172 / #185 / #224 / #237 | longest-prefix match ทีละข้อ | ✅ ถูกหมด |
| V3#102 / #238 / #275 | hello/dead ของ R1 เทียบ R2 (15/20, 15/40, 10/40) | ✅ A / D / A |
| V3#145 | exhibit เป็น `ND ::/0 [2/0]` → floating ต้อง AD > 2 | ✅ B (AD 3) |
| V3#157 / #306 | EUI-64 จาก MAC `12a0.abdd.0002` → `10A0:ABFF:FE…`, `5000.0004.0000` → `5200:00FF:FE04:0` | ✅ A / D |
| V3#171 | มี `B* 0.0.0.0/0` + `D EX` + `B 10.90.0.0/16 via Null0` | ✅ D |
| V3#192 | exhibit มี RSA key อยู่แล้ว + user privilege 1 → ต้อง `transport input ssh` + `enable secret` | ✅ A,D |
| V3#260/261/270/272/280 | ชุด `show interface` — runts / txload 255 / collisions ทั้งที่ full-duplex / output queue 185 | ✅ bad NIC / high throughput / duplex mismatch / high throughput / queueing |
| V3#284 · V1#710 | ไล่ next-hop ของทุกปลายทางตามตาราง Router1 | ✅ ครบ 6 ช่อง |
| V3#243 | 10.10.10.16, 10.10.13.129, 10.10.100.128 ไม่ match route ใด → default (Internet) | ✅ |
| V3#30 / #68 · V1#935 | mask ของแต่ละ prefix ตรงกับ `show ip route` | ✅ |
| V1#861 | AD 1 = static, 90 = EIGRP, 110 = OSPF, 120 = RIP | ✅ |
| V1#538 | รายการไอเทมมีแค่ BPDU guard / DAI / root guard / VACL → double tagging เหลือ VACL | ✅ |
| V3#9 | ไอเทมในภาพไม่มีคำว่า "basic service set" → "distribution system" คือตัวที่เหลือ | ✅ |

---

## 2) รายการ "เฉลยถูก แต่โจทย์ต้นฉบับบกพร่อง" (13 รายการ — ไม่ต้องแก้เฉลย)

> ทั้งหมดนี้ **ตอบตามเฉลยปัจจุบันได้เลย** เพราะเป็นคำตอบที่ข้อสอบจริงยึด แต่ถ้าเจอในห้องสอบแล้วรู้สึกว่า "มันแปลก ๆ" — เพราะมันแปลกจริง ๆ ไม่ใช่เราเข้าใจผิด

| # | ข้อ | ประเด็น | ทำไมเฉลยปัจจุบันยังถือว่าถูก |
|:--|:---|:---|:---|
| 1 | **V3#109** | A (AD 115) กับ D (AD 100) เป็น host route `/32` เหมือนกัน → **ทำงานได้ทั้งคู่** เพราะ longest-prefix ตัดสินก่อน AD | เลือก A ให้ตรงกับ V1#655 / V1#1026 / V2#602 ที่เป็นโจทย์เดียวกัน — มีหมายเหตุอธิบายไว้ในแอปแล้ว |
| 2 | **V3#79** | ตัวเลือก D ไม่มี `lldp run` ระดับ global (LLDP ปิดโดยค่าเริ่มต้น) จริง ๆ แล้วคอนฟิกนี้ยังใช้ไม่ได้ | D เป็นตัวเลือกเดียวที่ใช้ LLDP + ไวยากรณ์ระดับ interface ถูก (`lldp transmit/receive`) — มีหมายเหตุในแอปแล้ว |
| 3 | **V3#159** | `channel-group 1 mode passive` ถูกวางไว้ใต้ `interface port-channel 1` ซึ่งของจริงต้องสั่งที่พอร์ตกายภาพ | D เป็นตัวเลือกเดียวที่เป็น **passive** = "ตอบแต่ไม่เริ่มเจรจา" ตามที่โจทย์ถาม |
| 4 | **V3#167** | ของจริง AP ส่ง **discovery request → management interface** ส่วนที่ส่งไป AP-Manager คือ **join request** | ในชุดตัวเลือกมีแต่ discovery request/response กับ DHCP → D คือคำตอบที่ข้อสอบยึด |
| 5 | **V3#10** | คำว่า "without negotiating" ขัดกับ LACP passive (passive ก็เจรจา เพียงแต่ไม่เริ่มก่อน) | ฝั่งตรงข้ามเป็น static (`mode on`) ซึ่งจับคู่กับ `active` ไม่ได้ → ต้องเปลี่ยนเป็น passive = C |
| 6 | **V3#32** | ตัวเลือก B ใช้ `login authentication default` ทั้งที่ไม่มี `aaa new-model` (ของจริงพิมพ์ไม่ผ่าน) | ตัวเลือกอื่นผิดหนักกว่า (A ใช้ RADIUS ที่ล่มไปแล้ว, C สั่ง `no login local`, D ไม่มี username) |
| 7 | **V3#49** | E = `::ffff:10.14.101.1/96` เป็น IPv4-mapped ไม่ได้ใช้เชื่อมเราเตอร์จริง ๆ | ในชุดนี้มีแค่ D กับ E ที่เป็น unicast (A ไวยากรณ์ผิด, B/C เป็น multicast) |
| 8 | **V3#104** | ทางเทคนิค `logging trap <n>` เป็น "ตัวกรอง" ไม่ได้ "ตั้ง severity" ให้ข้อความ → D ก็ฟังขึ้น | B คือสำนวนที่ ExamTopics/9tut ยึด และ D แคบเกินไป (พูดถึงแค่ stored locally) — มีหมายเหตุในแอปแล้ว |
| 9 | **V3#222 · V3#316 · V1#769 · V1#802 · V2#732** | ประโยค "leverages a RADIUS server … reverse Telnet session" ของจริงมีคำสั่ง `aaa authorization reverse-access` → ตีเป็น Authorization ก็ได้ | ชุดข้อสอบออกแบบให้แต่ละบริการมี 2 ประโยคพอดี ถ้ายกไป Authorization จะเหลือ 3 ประโยคแต่มี 2 ช่อง และ V1#802 จะไม่มีคำตอบที่ลงตัว → จัดเป็น Authentication ทั้งแอป |
| 10 | **V3#285 · V1#565 · V2#136** | "unable to serve as destination addresses" — link-local ใช้เป็น destination ได้จริงบนลิงก์เดียวกัน (เช่น ping fe80::1) | เป็นการจับคู่แบบ "ตัวที่เหลือ" (ULA กิน 3 ช่อง, FF02::5 เป็นตัวลวง) → ตอบตามนี้ถูกในห้องสอบ |
| 11 | **V2#803** | Ansible ถูกจัดเป็น **Orchestration** ส่วน Puppet เป็น Configuration Management — ในโลกจริง Ansible ก็เป็น config management | เป็นการจับคู่ตามเฉลยข้อสอบ (Puppet ผูกกับ config management ชัดกว่า) |
| 12 | **V3#272 vs V3#260** | exhibit ทั้งสองใบมี `1876 runts` เหมือนกัน ต่างกันที่ txload/rxload | #260 โหลด 1/255 → ตัวผิดปกติมีแค่ runts = bad NIC · #272 โหลด **255/255** + rate 200 Mbps = high throughput ✔ |
| 13 | **V3#227** | คำว่า "three JSON list values" หมายถึง "ค่าที่เป็น list มี 3 ตัว" ไม่ใช่ "จำนวนสมาชิกใน list" (ซึ่งมี 6) | นับ object 1 + key 3 + array 3 → D |

---

## 3) สิ่งที่ตรวจแล้วไม่พบปัญหา (สรุปตามหมวด)

* **Routing / longest-prefix / AD** — ไล่คำนวณเองทุกข้อที่มีตาราง routing (~20 ข้อ) ✔ รวมถึง floating static (V3#145 AD 3, V3#179 AD 171 สำหรับ EIGRP external, V3#247 AD 121 สำหรับ RIP)
* **Subnetting** — usable range, last usable IP, wildcard mask, prefix→mask ทุกข้อ ✔ (V3#69, #123, #201, #230, V1#52, V1#564, V2#5, V2#132)
* **IPv6** — GUA/ULA/Link-local/Multicast/Anycast, EUI-64, prefix length ทุก drag (41 ข้อ) ✔
* **STP / EtherChannel / Port-security / VLAN-trunk** ✔
* **Wireless (WLC, AP modes, WPA2/WPA3, 802.11r/k/v, FlexConnect)** ✔
* **Automation (JSON/REST/Ansible/Puppet/Chef/DNA Center/SDN API)** ✔
* **AAA / Security / Syslog / SNMP / QoS / Cloud / Virtualization** ✔

---

## 4) การเติมหมายเหตุ (ดำเนินการแล้ว 10 ส.ค. 2026)

ไม่มีเฉลยข้อไหนที่ "ผิดตามหลัก CCNA" จึง **ไม่มีการแก้เฉลยแม้แต่ข้อเดียว** — สิ่งที่ทำคือเติม *หมายเหตุเตือน* ในคำอธิบายเท่านั้น

### ✅ เติมหมายเหตุใหม่ — 5 รายการ (7 ข้อใน 3 ไฟล์)

| รายการ | ข้อที่แก้ไฟล์ | หมายเหตุที่เติม |
|:---|:---|:---|
| 5 | `questionsV3.ts` V3#10 | "without negotiating" กำกวม — LACP passive ยังเจรจาอยู่ แต่ไม่เริ่มก่อน; ถ้าตีความว่าไม่เจรจาเลยต้องเป็น mode on ซึ่งจับคู่กับ active ไม่ได้ |
| 6 | `questionsV3.ts` V3#32 | `login authentication default` ต้องมี `aaa new-model` ก่อนถึงพิมพ์ผ่าน และ default list ก็ยังชี้ RADIUS ที่ล่ม — ของจริงต้องเป็น `line con 0` + `login local` |
| 10 | `questionsV3.ts` V3#285 · `questions.ts` V1#565 · `questionsV2.ts` V2#136 | link-local ใช้เป็นปลายทางได้จริงบนลิงก์เดียวกัน (ping FE80::1, OSPFv3/NDP) — ที่ทำไม่ได้คือเป็นปลายทางของทราฟฟิกที่ต้องเราต์ข้ามลิงก์ |
| 12 | `questionsV3.ts` V3#272 | exhibit ใบนี้มี 1876 runts เหมือนข้อ bad NIC เป๊ะ ๆ — ตัวชี้ขาดคือ txload/rxload 255/255 พร้อมลำดับการไล่อาการทั้งชุด |
| 13 | `questionsV3.ts` V3#227 | "JSON list values" = จำนวน list (3) ไม่ใช่จำนวนสมาชิกในลิสต์ (6) |

### ℹ️ ไม่ต้องเติม — 4 รายการที่มีหมายเหตุครอบคลุมอยู่แล้ว

ตอนเขียนรายงานรอบแรกระบุว่าทั้ง 9 รายการยังไม่มีหมายเหตุ ซึ่งคลาดเคลื่อน — เปิดดูคำอธิบายจริงแล้วพบว่า 4 รายการนี้มีอยู่แล้วและเนื้อหาตรงประเด็น จึงไม่เติมซ้ำให้รก:

* **รายการ 3 — V3#159:** "💡 ข้อสังเกต: คำสั่ง channel-group ตามหลักควรพิมพ์ใต้อินเทอร์เฟซกายภาพ ไม่ใช่ใต้ port-channel …"
* **รายการ 4 — V3#167:** "⚠️⚠️ โจทย์ข้อนี้เขียนกำกวม …" + อธิบายว่าบน WLC รุ่นใหม่ management ทำหน้าที่ AP-Manager ในตัว
* **รายการ 7 — V3#49:** "⚠️ หมายเหตุ: ข้อนี้เป็นคำถามที่เขียนไม่ดี — ::ffff:x.x.x.x ใช้ภายใน stack ของโฮสต์ …"
* **รายการ 11 — V2#803:** "⚠️ ข้อควรทราบ: ในความเป็นจริง Ansible ทำได้ทั้งสองอย่าง …"

### ผลตรวจหลังแก้
`tsc -b` ✅ ผ่าน · `vite build` ✅ ผ่าน · ดึงข้อมูลซ้ำได้ V1 1242 / V2 808 / V3 317 และ drag 115 + 85 + 49 = 249 ✅ ครบเท่าเดิม
