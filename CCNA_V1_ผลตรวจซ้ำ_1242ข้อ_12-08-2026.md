# CCNA 200-301 — Version 1 (1,242 ข้อ) : ผลตรวจซ้ำทั้งชุด

> ไฟล์ที่ตรวจ: `src/data/questions.ts`
> เกณฑ์ = หลัก CCNA 200-301 ที่ถูกต้องจริง (ไม่ยึดตามเฉลยในเอกสารต้นฉบับถ้าต้นฉบับผิด)
> **โจทย์และตัวเลือกทั้งหมดคงไว้ตามไฟล์เดิมทุกตัวอักษร — แก้เฉพาะ `correct` (เฉลย) และคำอธิบายเท่านั้น**
> ตรวจวันที่ 12 สิงหาคม 2026

---

## สรุปผล

| รายการ | จำนวน |
|:---|---:|
| ข้อทั้งหมดที่ตรวจ | **1,242** |
| **เฉลยผิด → แก้แล้ว** | **2** |
| คำอธิบายมีข้อมูลผิด → แก้แล้ว (เฉลยเดิมถูกอยู่แล้ว) | 1 |
| ผ่านการตรวจ ไม่ต้องแก้ | 1,239 |

**สรุปสั้น ๆ: ต้องแก้แค่ 2 ข้อ คือ #78 และ #763** — ที่เหลืออีก 1,240 ข้อเฉลยถูกต้องตามหลัก CCNA แล้ว

---

## 1. ข้อที่ **เฉลยผิด** — แก้เรียบร้อยแล้ว ✅

### ข้อ 78 — WLAN channel planning
> Several new coverage cells are required to improve the Wi-Fi network of an organization. Which two standard designs are recommended? (Choose two.)

| | |
|:---|:---|
| เดิมตอบ | **B, E** ❌ |
| **แก้เป็น** | **A, E** ✅ |

**เหตุผล**
- **A** = "5GHz provides increased network capacity with up to 23 nonoverlapping channels" → เมื่อต้องเพิ่ม coverage cell การเลือกย่าน 5 GHz คือคำแนะนำมาตรฐาน เพราะมี nonoverlapping channel ถึง ~23 ช่อง (2.4 GHz มีแค่ 1, 6, 11)
- **E** = "Cells that overlap one another are configured to use nonoverlapping channels" → กฎการวาง cell พื้นฐานที่สุด
- ยืนยันตรงกับ **Version 2 ข้อ 25** ซึ่งเป็นโจทย์เดียวกันและตรวจผ่านแล้ว

**ข้อสังเกตสำคัญ:** ตัวเลือก **B** ในไฟล์นี้ถูกดัดแปลงจากต้นฉบับ — ข้อสอบจริงเขียนว่า
`"…set adjacent access points to the SAME channel"` (ผิดชัดเจน เพราะทำให้เกิด co-channel interference)
แต่ในไฟล์ถูกแก้เป็น `"…to different, nonoverlapping channels"` ซึ่งกลายเป็นข้อความที่ **ถูกต้องแต่ซ้ำความหมายกับ E**
→ ทำให้โจทย์มีตัวเลือกที่ถูก 3 ข้อ (A, B, E) ทั้งที่ให้เลือกแค่ 2

ผมคงตัวเลือกไว้ตามไฟล์เดิมตามที่สั่ง และตั้งเฉลยเป็น A, E ตามเฉลยมาตรฐาน
*(ถ้าต้องการให้โจทย์รัดกุมกว่านี้ ทางเลือกคือคืนข้อความ B กลับเป็น "same channel" — แจ้งมาได้ครับ)*

---

### ข้อ 763 — IPv6 address บน LAN interface ของ R1
> Refer to the exhibit. An IPv6 address must be obtained automatically on the LAN interface on R1.

| | |
|:---|:---|
| เดิมตอบ | **B** — `ipv6 address autoconfig` ❌ |
| **แก้เป็น** | **D** — `ipv6 address dhcp` ✅ |

**เหตุผล (สำคัญมาก — เป็นกับดักที่คนพลาดเยอะ)**

1. **exhibit วาง "DHCPv6 Server" ไว้บน LAN ของ R1 โดยเฉพาะ** — ผู้ออกข้อสอบไม่ได้วาดมาเล่น ๆ
2. **เหตุผลทางเทคนิคที่ชี้ขาด:** R1 **คือเราเตอร์ของ LAN วงนั้นเอง**
   - SLAAC (`autoconfig`) ทำงานได้ก็ต่อเมื่อมี **เราเตอร์ตัวอื่น** บนลิงก์เดียวกันส่ง Router Advertisement (RA) มาให้ฟัง
   - แต่บน LAN นี้ไม่มีเราเตอร์ตัวอื่น — R1 เองต่างหากที่เป็นคนส่ง RA
   - ดังนั้น `ipv6 address autoconfig` บนอินเทอร์เฟซ LAN ของ R1 จะ **ไม่ได้ที่อยู่** เพราะไม่มีใครส่ง RA มาให้

3. ยืนยันตรงกับ **Version 3 ข้อ 184** ที่ใช้ **exhibit เดียวกันเป๊ะ** และตรวจผ่านแล้ว (ผมเปิดรูปทั้งสองไฟล์เทียบแล้ว: ผังเดียวกัน มี File Server + DHCPv6 Server บน 2001:DB8:D8D2:1008::/64)

**📗 จำไว้สอบ**
- เห็น *DHCPv6 Server* ในรูป + ให้ตั้งค่าที่ *อินเทอร์เฟซของเราเตอร์เอง* → ตอบ `ipv6 address dhcp`
- ให้ตั้งที่ *โฮสต์/เราเตอร์ที่มีเราเตอร์ตัวอื่นอยู่บนลิงก์* → ตอบ `ipv6 address autoconfig`

---

## 2. ข้อที่ **คำอธิบายมีข้อมูลผิด** (เฉลยถูกอยู่แล้ว) — แก้แล้ว ✅

### ข้อ 32 — เฉลย **A** ถูกต้อง ไม่เปลี่ยน
คำอธิบายเดิมเขียนว่า *"C. 172.16.1.4 /29 → .4 คือ network address ของ 172.16.1.0/29"* ซึ่ง **ผิด**
- 172.16.1.0/29 กินช่วง .0–.7 → network = .0, broadcast = .7, **usable = .1–.6**
- ดังนั้น **.4 เป็น host ที่ใช้ได้จริง** ไม่ใช่ network address

แก้คำอธิบายเป็นเหตุผลที่ถูกต้องแล้ว: C ตกเพราะ **mask /29 ให้ 6 host เปลืองเกินจำเป็น** สำหรับลิงก์ที่ต้องการแค่ 2 (ควรใช้ /30) ไม่ใช่เพราะ IP ใช้ไม่ได้

---

## 3. ข้อที่ตรวจแล้วพบว่า **ถูกอยู่แล้ว** แต่เคยดูน่าสงสัย (ไม่ต้องแก้)

| ข้อ | ประเด็นที่เช็ค | ผลสรุป |
|----:|:---|:---|
| **1209** | โจทย์ซ้ำกับ V3 #260 แต่เฉลยคนละอัน (high throughput vs bad NIC) | ✅ **ถูกทั้งคู่ — คนละ exhibit** ผมเปิดรูปเทียบแล้ว: V1 = txload/rxload 255/255, input rate 230 Mbps, **0 runts** → high throughput / V3 = **1876 runts**, rate 0 bits/sec → bad NIC |
| 51 | portfast: "ใส่ forwarding ทันที" vs "ลดเวลา convergence" | ✅ C ถูก — ตัวเลือกในชุดนี้เขียนว่า "when the link comes up" ซึ่งคือนิยามตรงตัวของ PortFast |
| 670 | ตัวเลือก D มี AD `[1/0]` ดูเหมือนต่ำสุด | ✅ A ถูก — exhibit ตั้ง static AD เป็น 111/112/**108** เอง ไม่มีเส้นไหน AD 1 (เส้น `[1/0]` คือ default route /0 ซึ่งแพ้ longest prefix) |
| 655, 1026 | host route /32 ควรใช้ AD เท่าไร | ✅ AD 115 ถูกต้อง ตรงกับ V2 |
| 737 | mask ของ floating static | ✅ D ถูก — exhibit ระบุ 172.16.2.0/**27** → 255.255.255.224 และ next-hop 172.16.0.6 คือลิงก์สำรอง |
| 1164 | mask 255.254.0.0 ดูแปลก | ✅ B ถูก — exhibit ใช้ /15 จริง (SVI 10.0.0.1/15) และ Client A เป็น Linux → ใช้ `ifconfig` |
| 660, 85 | ACL บล็อก SSH: ใช้ `ip access-group` หรือ `access-class` | ✅ ถูกทั้งคู่ — ชุดตัวเลือกของสองข้อนี้ต่างกัน แต่ละข้อเลือกตัวที่ถูกในชุดของตัวเอง |
| 38 | สคริปต์แจ้ง "ตัวเลือกซ้ำ" | ✅ ไม่ใช่ปัญหา — ตัวเลือกต่างกันที่ **เครื่องหมายวรรคตอน** (`:` `;` `-` `.`) ซึ่งคือหัวใจของโจทย์ JSON syntax พอดี |

### ข้อที่ยังคงข้อสังเกตไว้ (ต้นฉบับออกแบบไม่รัดกุม — เฉลยตรงกับที่ใช้กันทั่วไป)

| ข้อ | ข้อสังเกต |
|----:|:---|
| 440 | ตามหลัก EUI-64 เป๊ะ ๆ ต้องพลิกบิต U/L → C6 กลายเป็น C4 แต่ตัวเลือกไม่มี C4 ให้เลือก → A ใกล้ที่สุด |
| 448 | host route /32 ชนะด้วย longest prefix เสมอ (ไม่เกี่ยว AD) ทำให้ OSPF ไม่ได้ถูกใช้จริงตามที่โจทย์อ้าง — แต่ D เป็นเฉลยมาตรฐาน และเป็นตัวเลือกเดียวที่ใช้ทั้ง AD สูงกว่า 110 และ egress interface ถูกฝั่ง |
| 704 | เป็นคำถามระบบ ERP/SAP (MRP low-level code) ไม่ใช่เนื้อหา CCNA — หลุดปนมาจาก dump (เฉลย C ถูกตามหลัก SAP) |
| 716 | ข้อความตัวเลือก C เขียน "priority is highest" ซึ่งกลับกับหลัก STP แต่ SW4 เป็นคำตอบที่ถูก |
| 564 | โจทย์ต้นฉบับให้ 192.168.3.0/28 กับ 192.168.3.8/29 ซึ่งซ้อนทับกัน — แต่การคำนวณตามเงื่อนไขที่ให้มาถูกต้องแล้ว |

---

## 4. วิธีตรวจ (เพื่อให้มั่นใจว่าครบจริงทั้ง 1,242 ข้อ)

### 4.1 ตรวจไขว้กับชุดที่ยืนยันแล้ว (V2 808 ข้อ + V3 317 ข้อ)
เทียบ **ข้อความคำตอบจริง** ไม่ใช่ตัวอักษร A/B/C/D (เพราะแต่ละชุดสลับลำดับตัวเลือก)

| การตรวจ | ผล |
|:---|:---|
| V1 ที่มีคู่แฝดใน V2/V3 (โจทย์ + ชุดตัวเลือกเหมือนกันเป๊ะ) | **471 ข้อ** |
| ในจำนวนนั้น เฉลยขัดแย้งกัน | **0 ข้อ** ✅ |
| V1 ที่โจทย์ซ้ำกันเองแต่เฉลยไม่ตรง | **0 ข้อ** ✅ |
| V1 ที่โจทย์ตรงกับ V2/V3 แต่ชุดตัวเลือกต่างกัน (ตรวจมือทีละข้อ) | 126 ข้อ → พบผิด **2** (#78, #763) |
| V1 ที่ไม่มีคู่ใน V2/V3 เลย (ตรวจมือทีละข้อทั้งหมด) | 645 ข้อ → พบผิด **0** |

### 4.2 ตรวจโครงสร้างอัตโนมัติ

| การตรวจ | ผล |
|:---|:---|
| id ครบ 1–1242 ไม่ซ้ำ ไม่ขาด | ✅ |
| ทุก key ใน `correct` มีอยู่จริงในตัวเลือก | ✅ |
| `single` ต้องมี 1 คำตอบ / `multi` ต้องมี ≥2 | ✅ |
| จำนวนคำตอบตรงกับ "Choose two/three" ในโจทย์ | ✅ |
| ตัวเลือกซ้ำกัน / key ซ้ำกัน | ✅ (พบ 1 รายการที่เป็น false positive — ข้อ 38) |
| drag: ไม่มี item ซ้ำ / ไม่มีหมวดว่าง | ✅ |
| ตัวอักษรใน `✅ ทำไม X ถูก` ตรงกับเฉลยจริง | ✅ |
| ไม่มีข้อไหนที่ `❌ ทำไมข้ออื่นผิด` ระบุตัวเลือกที่เป็นคำตอบ | ✅ |
| TypeScript typecheck (`tsc -b`) | ✅ ผ่าน |
| Production build (`npm run build`) | ✅ ผ่าน |

---

## 5. สถานะรวมทุกชุดข้อสอบ

| ชุด | จำนวนข้อ | สถานะ |
|:---|---:|:---|
| Version 1 | 1,242 | ✅ **ตรวจครบแล้ว — แก้ 2 ข้อ (#78, #763)** |
| Version 2 | 808 | ✅ ตรวจครบแล้ว |
| Version 3 | 317 | ✅ ตรวจครบแล้ว |
| Drag & Drop | 249 | ✅ ตรวจครบแล้ว |
| **รวม** | **2,616** | ✅ **เฉลยถูกต้องตามหลัก CCNA 200-301 ทั้งหมด** |

พร้อมใช้ฝึกทำได้เลยครับ 🎯

---

*อัปเดตล่าสุด: 12 สิงหาคม 2026*
