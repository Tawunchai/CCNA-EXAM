# BIG CCNA V4 — Full check ทั้ง 2085 ข้อ

เริ่ม 2026-08-30 · ไฟล์เป้าหมาย `src/data/questionsV4.ts`

ตรวจทีละข้อ: อ่านโจทย์ + ตัวเลือก + เฉลย เทียบกับหลัก CCNA 200-301 และเปิดภาพ exhibit เมื่อคำอธิบายไม่ได้อ้างค่าจากรูปอย่างชัดเจน

## สถานะรวม

| ช่วง | สถานะ | แก้ไข |
|---|---|---|
| 1–1500 | ✅ ตรวจครบแล้ว | #552, #665, #1224 |
| 1501–2085 | ✅ ตรวจครบแล้ว | #1508, #1680, #1728 |
| **รวม 2085 ข้อ** | **✅ ตรวจครบทุกข้อแล้ว** | **แก้ 6 ข้อใน V4 + 2 ข้อในคลังอื่น** |

## สรุปข้อที่แก้ไข

| ข้อ | เดิม | แก้เป็น | เหตุผล |
|---|---|---|---|
| V4 #552 (drag) | E = PAT overload, F = static NAT | **E = static NAT, F = PAT overload** | สลับตำแหน่งกัน ไม่ตรงกับลำดับคอนฟิกจริงและกับ V1 #133 / V4 #1560 |
| V2 #435 (drag) | E = PAT overload, F = static NAT | **E = static NAT, F = PAT overload** | สำเนาเดียวกับ #552 |
| V4 #665 (drag) | ช่อง 4 = `violation shutdown` | **ช่อง 4 = `mac-address sticky`** | `violation shutdown` เป็นค่าเริ่มต้นอยู่แล้ว ส่วน "two **random** MAC" ชี้ไปที่ sticky — ตรงกับ V1 #492 / V2 #730 / V4 #1522 / #1717 |
| V4 #1224 | D straight-through | **A crossover** | โจทย์ระบุว่าขา 1,2 เป็นตัวรับทั้งสองฝั่ง → บทบาทขาเหมือนกัน ต้องไขว้สาย ตรงกับ V1 #943 / V4 #1844 |
| V4 #1508 | multi, A,B,C (Choose three) | **single, B (F0/11)** | exhibit แสดง `[90/144]` ไม่ใช่ `[90/1441]` metric ไม่เท่ากันจึงไม่แบ่งโหลด |
| V4 #1680 | D (zones 3 and 6) | **C (zones 3 and 4)** | โซน 3 กับ 6 อยู่คนละแถวและวงไม่ทับซ้อนกัน ส่วนโซน 3 กับ 4 ทับซ้อนและใช้ channel 11 เหมือนกัน |
| V1 #429 | D (zones 3 and 6) | **C (zones 3 and 4)** | สำเนาเดียวกับ #1680 |
| V4 #1728 (prompt) | "0 collisions" | **"6,664,241,695 collisions"** | ข้อความสรุป exhibit ในโจทย์ขัดกับภาพจริงและกับเฉลย B (duplex mismatch) |

## การตรวจอัตโนมัติทั้งคลัง (รันแล้ว ผ่าน)

รันครอบทั้ง 2085 ข้อ ไม่พบปัญหาเชิงโครงสร้าง:

- เฉลยชี้ไปยังตัวเลือกที่ไม่มีอยู่จริง → **0**
- `(Choose N)` ไม่ตรงกับจำนวนคำตอบ → **0**
- `kind: 'single'` แต่มีหลายคำตอบ / `kind: 'multi'` แต่มีคำตอบเดียว → **0**
- คำอธิบายบรรทัด `✅ ทำไม X ถูก` ระบุตัวอักษรที่ไม่ได้อยู่ในเฉลย → **0**
- โจทย์ซ้ำข้ามคลัง (V1/V2/V3/V4) ที่ **ภาพ exhibit เดียวกัน + ชุดตัวเลือกเดียวกัน** แต่เฉลยต่างกัน → **0**

หมายเหตุ: มี 68 ข้อที่คำอธิบายไม่มีบรรทัด `✅ ทำไม X ถูก` ตามรูปแบบมาตรฐาน — เป็นเรื่องรูปแบบการเขียน ไม่ใช่เฉลยขัดกัน ตรวจเนื้อหาด้วยตาแล้วทุกข้อที่ไล่ผ่าน

---

## บันทึกรายช่วง 100 ข้อ

### 1–100 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

ไล่ครบทุกข้อ จุดที่คำนวณยืนยันด้วยมือ:

- **#5** ช่วง host ของ 172.28.228.144 ทั้ง 5 มาสก์ (/18 /21 /23 /25 /29) ถูกทุกบรรทัด
- **#14** `/30` — 10.2.1.3 เป็น broadcast จึงใช้ไม่ได้, 209.165.201.2 ถูก
- **#34** 4 ชั้น ผู้ใช้สูงสุด 29 คน → /27 ต่อชั้น รวมเป็น /25 summary
- **#90** เลือกด้วย AD ต่ำสุด 108 (static ที่ตั้ง AD เอง) ชนะ OSPF 110
- **#98 / #101** การย่อ IPv6 และ EUI-64 (แทรก FFFE) ถูก
- **#109** last usable ของ /26 /28 /27 = .126 / .94 / .158 ถูกทุกตัว

### 101–200 · ⚠️ เฉลยถูกทั้งหมด แต่ไปเจอว่าข้ออื่นที่ใช้ภาพเดียวกันผิด

- **#173** (co-channel interference) เฉลย **C = zones 3 and 4** ✅ **ถูก** — เปิดภาพวิเคราะห์ลายเส้นแล้ว: `channel 1 = เส้นทแยง /`, `channel 6 = เส้นทแยง \`, `channel 11 = ลายตาราง` ผังที่ได้คือ

  | | | | |
  |---|---|---|---|
  | Zone1 = ch 1 | Zone2 = ch 6 | **Zone3 = ch 11** | **Zone4 = ch 11** |
  | Zone5 = ch 6 | Zone6 = ch 11 | Zone7 = ch 1 | Zone8 = ch 6 |

  โซน 3 กับ 4 อยู่ติดกันในแถวเดียวกัน วงทับซ้อนกันและใช้ช่อง 11 เหมือนกัน → co-channel
  ส่วนโซน 3 กับ 6 เยื้องกันคนละแถว ระยะศูนย์กลาง ≈ 191 px เทียบกับเส้นผ่านศูนย์กลาง ≈ 160 px → **วงไม่แตะกันเลย** จึงกวนกันไม่ได้

- **#159** (data plane) เฉลย B ตรงกับ V1 #442 และ V2 #159 → ปล่อยไว้
- **#166** 30+1 VLAN จาก Class C → ต้อง /29 (255.255.255.248) ถูก
- **#185** ต้องใช้ `priority 0` ไม่ใช่ `root primary` (ซึ่งตั้งแค่ 24576) ถูก

**🔧 แก้ไปแล้วจากการตรวจช่วงนี้ (คนละช่วง แต่เป็นโจทย์เดียวกัน):**

| ข้อ | เดิม | แก้เป็น |
|---|---|---|
| V4 #1680 | D (zones 3 and 6) | **C (zones 3 and 4)** |
| V1 #429 | D (zones 3 and 6) | **C (zones 3 and 4)** |

### 201–300 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

ช่วงนี้เป็นเนื้อหา switching / STP / EtherChannel / VLAN / wireless เป็นหลัก

- **#210** default VLAN = 1 และ 1005 (1002–1005 เป็น default VLAN ของ FDDI/Token Ring) ถูก
- **#252** AP ใช้ **CDP** เจรจาขอกำลังไฟ PoE จากสวิตช์ ถูก
- **#261** drag สถานะ forwarding ของ Rapid PVST+ ตรงกับ V1 #516, V1 #615, V2 #261 ทุกช่อง
- **#279** ตรวจซ้ำเพราะวลี "without negotiating" ชวนสับสน — เฉลย D (passive) ถูก เพราะ passive คือฝั่งที่ *ไม่เริ่ม* เจรจา และตรงกับ V1 #602, V2 #279, V3 #10
- **#284** drag threat-mitigation: double tagging→VACL, ARP spoofing→DAI, superior BPDU→root guard, BPDU บน PortFast→BPDU guard ถูก
- **#299** เลือก passive ได้ด้วยการตัดตัวเลือก (มีแค่ D ที่เป็น passive)
- **#303** ใน Rapid PVST+ สถานะคือ discarding/learning/forwarding → PortFast ข้าม **learning** ถูก
- **#325** LAG บน WLC ต้องใช้ `channel-group mode on` เท่านั้น ถูก

### 301–400 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#331** ตอบ D ด้วยการตัดตัวเลือก (อีกสามข้อเป็นข้อความเท็จทั้งหมด)
- **#339** ไม่มีตัวเลือก "join request" จึงตอบ Discovery request ตามเฉลยมาตรฐาน
- **#346** 10.10.1.22 → มีแต่ `10.10.1.20/30` (.20–.23) ที่ครอบคลุม ถูก
- **#350** เหมือน #1541 → OSPF /30 (ชนะ AD) + EIGRP /32 (ไม่มีคู่แข่ง)
- **#372** "establishes neighbor without forming adjacency" → modify hello interval ตรงกับตระกูล #1604/#1712
- **#382** variance 2 × FD 3072 = 6144 พอดีกับเส้นที่สอง ถูก
- **#390** HSRP priority เท่ากันโดยดีฟอลต์ → ตัดสินด้วย IP address ถูก

### 401–500 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#412** 172.16.3.14 เข้า /28 (.0–.15) จึงไปทาง .254 ไม่ใช่ /24 — ต่างจาก #411 ที่ปลายทาง 172.16.0.14 ไม่เข้า route ใดเลยจึงใช้ default ✔ สองข้อนี้ไม่ขัดกัน
- **#437** FHRP ตรวจซ้ำเพราะตัวเลือกกำกวม — "forwards multicast hello messages between routers" ตรงกันทั้ง V1 #199, V1 #677, V2 #467, V2 #566, V4 #1486
- **#462** ใช้ host route /32 ไปยังเซิร์ฟเวอร์เท่านั้น เกตเวย์จึง ping ไม่ถึง ถูก
- **#476** 600 เซิร์ฟเวอร์ → /22 (1022 hosts) ถูก
- **#485** longest prefix /25 ชนะ /24 และ /16 ถูก
- **#494** priority 0 + ACL อนุญาต protocol 89 เฉพาะจาก R2 ถูกครบทั้งสองเงื่อนไข
- **#505** HSRP group 14 → `0000.0C07.AC0E` (14 = 0x0E) ถูก
- **#522** `logging trap 3` → ส่ง level 0–3 = emergency, alert, critical, error → ในตัวเลือกคือ emergency/critical/error ถูก

### 501–600 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#505** HSRP group 14 → virtual MAC `0000.0C07.AC0E` ถูก
- **#522** `logging trap 3` → emergency / critical / error ถูก
- **#533** ตรวจซ้ำเพราะมีสองสำนวนในคลัง — สำนวน "network device has restarted" กับสำนวน "interface line has changed status" เป็น**คนละชุดตัวเลือก** จึงเฉลยต่างกันได้ และตรงกันทุกคลัง (V1 #29/#67/#1038, V2 #632/#636, V4 #529/#533/#1894)
- **#552** drag static NAT — **แก้แล้วในรอบนี้** (E/F สลับกัน) ดูตารางท้ายไฟล์
- **#555** printer ที่ตั้ง static IP บน Wi-Fi → ต้องเปิด **passive client** (ต่างจาก #842 ที่เป็นเรื่อง Static IP Tunneling บน WLAN) ทั้งคู่ถูก ไม่ขัดกัน
- **#562** ต้องตั้ง `debug` (7) จึงจะได้ข้อความระดับ informational (6) เพราะไม่มีตัวเลือก informational

### 601–700 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#650** DHCP snooping → "rate-limits certain traffic" ถูกตามเฉลยมาตรฐาน
- **#665** drag port security — **แก้แล้วในรอบนี้** (ช่อง 4 เป็น violation shutdown ผิด) ดูตารางท้ายไฟล์
- **#670** WPA3 เข้ารหัสด้วย **AES** ส่วน SAE เป็นวิธีแลกกุญแจ ไม่ใช่การเข้ารหัส — ต่างจาก #627/#686 ที่ถามเรื่อง "การป้องกัน/การพิสูจน์ตัวตน" จึงตอบ SAE ทั้งสามข้อถูกในบริบทของตัวเอง
- **#677** เลือก D ด้วยการตัดตัวเลือก (C ใช้ `no login local` ซึ่งกลับด้าน)

### 701–800 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#760 vs #789** โจทย์ข้อความเดียวกัน ("function of the controller in an SDN") แต่**ชุดตัวเลือกต่างกัน** — #760 มี "making routing decisions" ส่วน #789 มี "setting packet-handling policies" ทั้งคู่ถูกในชุดของตัวเอง
- **#773** Puppet = configuration management, Ansible = orchestration ตรงตามชุด drag มาตรฐาน
- **#790** NETCONF ไม่โผล่ใน running-config จนกว่าจะตั้งพารามิเตอร์ → `netconf max-message 1000` ถูก

### 801–900 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#800** GBIC ใช้หัว SC และ SFP ใช้หัว LC → สาย **LC to SC** ถูก
- **#820** 1 Hz = 1 ครั้งต่อวินาที = 60 ครั้งต่อนาที ถูก
- **#824** anycast ใช้ที่อยู่ unicast ปกติ (มัก /128) → `2001:db8:0234:ca3e::1/128` ถูก
- **#848** ในตาราง port state ของ Cisco สถานะที่ "ประมวลผล BPDU แต่ไม่ส่งต่อเฟรมและไม่เรียน MAC" คือ **listening** (blocking แค่ *รับ* BPDU ไม่ได้ประมวลผล) ถูก
- **#850** ถ้อยคำในตัวเลือกเขียน "priority is highest" สลับกับความจริง แต่สวิตช์ที่ระบุ (SW4, priority 8192) ถูกต้อง และตรงกับ V1 #716 — คำอธิบายในระบบได้กำกับเรื่องถ้อยคำไว้แล้ว
- **#903 vs #419/#1660/#1505** ปลายทาง 172.16.0.202 เหมือนกันแต่ **exhibit คนละใบ** (metric 32445 กับ 38443) ทุกข้อถูกตาม exhibit ของตัวเอง

### 901–1000 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#916** EUI-64 ที่พลิกบิต U/L จริง (50 → 52) ถูก — ต่างจาก #101/#1687 ที่ชุดตัวเลือกไม่มีตัวที่พลิกบิต จึงต้องตอบตัวที่แทรก FFFE เท่านั้น (มีหมายเหตุกำกับไว้ในระบบแล้ว)
- **#938** drag ลำดับ AD: Static 1 < eBGP 20 < EIGRP 90 < OSPF 110 → เงื่อนไขทั้งห้าจับคู่ถูกหมด
- **#949** ฟิลด์ในเฮดเดอร์ IPv4 ที่ถูกแก้คือ **Type of Service** ส่วน DSCP เป็นบิตย่อยข้างใน ถูก
- **#994** "antivirus software" กับ V1 #779 "antimalware software" เป็นคนละชุดตัวเลือก ทั้งคู่ถูก

### 1001–1100 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#1057 vs #1993** โจทย์ port security บนโทรศัพท์ IP — ชุดตัวเลือกต่างกัน ชุดที่มี `vlan voice` ตอบ `vlan voice`, ชุดที่ไม่มีตอบ `vlan 4` ตรงกันทุกคลัง (V1 #661/#1202, V3 #128)
- **#1093** drag single-mode vs multimode — **ต้นฉบับบกพร่อง** ให้ตัวเลือกที่เป็นคุณสมบัติ single-mode มา 3 ใบสำหรับ 2 ช่อง วลี "is ideal over longer distances with little loss of integrity" จึงถูกบังคับให้ลงช่อง multimode ทั้งที่ตามหลักเป็นของ single-mode — คำอธิบายในระบบเตือนเรื่องนี้ไว้แล้วและอ้างอิงข้อ #1088 / #1137 / #1329 ที่จับคู่ถูก ✔ ไม่ต้องแก้
- **#1028** drag configuration management — ช่อง "easy-to-manage deployment option that may lack scalability" ได้ `pull` จากการตัดตัวเลือก

### 1101–1200 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- ข้อตระกูล "What is a characteristic of …" (SSID / encryption / private IPv4 / Layer 2 switch / frame switching / MAC learning) มีหลายสิบข้อที่ **ข้อความโจทย์เหมือนกันแต่ชุดตัวเลือกต่างกัน** ตรวจทีละข้อแล้วทุกข้อตอบถูกตามชุดของตัวเอง
- **#1120 / #1123 / #1136 / #1152 / #1172 / #1187** root bridge — เทียบ priority ก่อน MAC ถูกทุกข้อ

### 1201–1300 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#1224** สาย crossover — **แก้แล้วในรอบนี้** (เดิมตอบ straight-through) ดูตารางท้ายไฟล์
- **#1201 vs #1825** OfficeExtend vs FlexConnect — ชุดตัวเลือกต่างกัน ทั้งคู่ถูก
- **#1315** `default-router 192.168.20.253` = next-to-last usable ของ /24 ถูก

### 1301–1400 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#1364 / #1375 / #1383 / #1387** ข้อความโจทย์ระบุแต่ MAC ทำให้ดูเหมือนเฉลยผิด แต่ **เปิดภาพแล้วพบว่า exhibit แสดง STP Priority** ที่ prompt ไม่ได้เขียนไว้ (เช่น #1387 → SW1 32768 / SW2 4096 / SW3 8192 / SW4 12288) เฉลยถูกทุกข้อ
- **#1379** สถานะที่ไม่รับ BPDU และไม่อัปเดตตาราง MAC = **disabled** ถูก
- **#1321 vs #1479** โจทย์เดียวกันแต่คนละชุดตัวเลือก — ชุดหนึ่งตอบ "query a DNS server" อีกชุดตอบ "sends a broadcast" (เพราะยังไม่ได้ตั้ง `ip name-server`) ทั้งคู่ถูก

### 1401–1500 · ✅ เฉลยถูกทั้งหมด — ไม่ต้องแก้

- **#1444** ข้อความหัวโจทย์เขียนว่า "networking parameters" แต่ช่องจริงคือ Connection Oriented / Connectionless และเนื้อหาถูกต้อง (ปัญหาถ้อยคำเท่านั้น ไม่กระทบเฉลย)
- **#1485** ตาราง 802.11 a/b/g/n/ac ถูกทุกช่อง
- **#1498** ปิดไม่ให้เพิ่ม OSPF router บนลิงก์ → `ip ospf network point-to-point` ถูก

## ภาคผนวก — ข้อ port security drag (#665 · #1522 · #1717 · V1 #492 · V2 #730)

**เฉลย: ช่อง 4 = `switchport port-security mac-address sticky`** (ทั้ง 5 สำเนาตรงกัน)

โจทย์ข้อนี้ถกเถียงกันมากว่าช่อง 4 ควรเป็น `sticky` หรือ `violation shutdown` ข้อสรุปหลังตรวจซ้ำ:

**ต้นตอคือโจทย์บกพร่อง** — คำสั่งที่จำเป็นจริงมีแค่ 3 (`mode access`, `port-security`, `maximum 2`) แต่มี 4 ช่อง จึงถูกบังคับให้เติมคำสั่งที่ไม่จำเป็นเข้าไปหนึ่งใบไม่ว่าทางไหน

**เหตุผลที่ตัดสินว่าเป็น sticky — โครงสร้างชุดตัวเลือก:**

| กลุ่ม | ตัวเลือก | เลือก |
|---|---|---|
| บังคับ | `mode access` / `port-security` / `maximum 2` | ทั้งสาม |
| ได้ MAC มาอย่างไร (3 ใบ เลือก 1) | MAC เจาะจง ×2, `mac-address sticky` | **sticky** ← คำว่า "random" เป็นตัวชี้ |
| ทำอะไรตอนละเมิด | `violation shutdown` | ตัวลวง (เป็นดีฟอลต์อยู่แล้ว) |

ถ้าเฉลยเป็น `violation shutdown` จะแปลว่าตัวเลือกตระกูล `mac-address` ทั้งสามใบเป็นตัวลวงหมด ซึ่งไม่ใช่วิธีวางตัวเลือกของข้อสอบ

**ข้อโต้แย้งที่ถูกต้องในทางเทคนิค (บันทึกไว้ในคำอธิบายของทั้ง 5 สำเนาแล้ว):** `sticky` เขียน MAC ลง running-config, ไม่ age out, อยู่รอดตอนพอร์ต down → ถอดเครื่องเดิมเสียบเครื่องใหม่แล้ว violation ทันที ซึ่งเป็นข้อจำกัดที่โจทย์ไม่ได้ขอ ถ้าตัดสินด้วยหลักวิศวกรรมล้วน ๆ การเติม `violation shutdown` (ซ้ำซ้อนแต่ไม่ทำให้อะไรพัง) ปลอดภัยกว่า

**วิธีจำสองชั้น:** เข้าห้องสอบตอบ sticky · ทำงานจริงจำว่า sticky = ล็อก MAC ถาวร

<!-- ช่วงถัดไปจะถูกเพิ่มต่อท้ายส่วนนี้ -->

---

# รอบที่ 2 — ตรวจซ้ำโดย **เปิดภาพ exhibit ทุกใบ** (2026-08-31)

รอบแรก (2026-08-30) เป็นการตรวจระดับข้อความ เปิดภาพเฉพาะข้อที่คำอธิบายดูบาง
รอบนี้เปิดภาพ exhibit **ทุกใบ** (979 ภาพใน V4 · ภาพไม่ซ้ำกัน 967 ใบ) แล้วคิดคำตอบเองก่อนเทียบเฉลย

ผลตรวจอัตโนมัติก่อนเริ่ม: `audit.cjs` → HARD 0 · `xcheck.cjs` → conflict 0

## บันทึกรายช่วง 100 ข้อ (รอบเปิดภาพ)

### 1–100 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 22 ใบ

- **#1** ตาราง route มี `10.10.13.0/25 [110/2]` ครอบ 10.10.13.10 → เป็น *network route* ไม่ใช่ host route (ไม่มี /32 ในตาราง) เฉลย B ถูก
- **#2** Host A = 10.10.13.214 · ตารางมี /25, 128/28, 144/28, 160/29, 208/29 → 214 อยู่ใน **10.10.13.208/29** เท่านั้น เฉลย D ถูก
- **#5** เช็กครบ 5 มาสก์จากภาพ: /18→192.1-255.254 · /21→224.1-231.254 · /23→228.1-229.254 · /25→228.129-228.254 · /29→228.145-228.150 ตรงทุกช่อง
- **#12** ภาพเป็น output ของ `ip route`/`ip addr` บน Linux: default 192.168.1.193 · src 192.168.1.200 · MAC 00:0C:22:83:79:A3 · /26 = 255.255.255.192 ตรงทุกช่อง
- **#16** ภาพ: `ip address 192.168.16.143 255.255.255.240` → `Bad mask /28` · 128–143 บล็อก 16 ทำให้ .143 เป็น broadcast เฉลย D ถูก
- **#35** ภาพแสดง subnet ที่ใช้แล้ว 0/28, 16/28, 64/26 → /27 ว่างที่ต่ำสุดคือ **32/27** เฉลย D ถูก
- **#53** ภาพเผยว่า Router1 มีสองอินเทอร์เฟซเป็น 192.168.1.1/24 และ 192.168.1.2/24 — ซับเน็ตทับกันบนเราเตอร์เดียว → เฉลย C (เราเตอร์ไม่ยอมรับ) ถูก
- **#62** ไม่มี NAT ในภาพ → IP ต้นทาง/ปลายทางคงเดิมตลอดทาง เฉลย C ถูก
- **#75** ภาพ SiteA: `reliability 166/255` (SiteB 255/255) → มี error สะสมจริง เฉลย A ถูก · media type ทั้งสองฝั่งเป็น SFP-LR เหมือนกัน จึงตัด C/D
- **#85** ภาพ: PC-A 10.10.10.0/24 · PC-B 10.10.100.0/24 → คนละเน็ตเวิร์ก ต้องใช้เราเตอร์ เฉลย A ถูก
- **#90** ภาพแสดง static 3 เส้นสู่ 10.10.13.0/25 ที่ตั้ง AD เอง 111 / 112 / **108** และ OSPF (110) → AD ต่ำสุด 108 via 10.10.10.10 ชนะ เฉลย B ถูก
- **#93** ภาพ: PC1 ตั้งมือ 100/full แต่สวิตช์ Fa0/1 เป็น `auto/auto` → เจรจาไม่สำเร็จ สวิตช์ตกไป half → **duplex mismatch** เฉลย B ถูก
- drag ที่เปิดภาพยืนยันชุดตัวเลือกครบถ้วน: #3 #5 #12 #38 #44 #47 #71 #82 #95 #96 #99 — ทุกใบตรงกับเฉลยในระบบ

### 101–200 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 32 ใบ

- **#101** ภาพระบุ `fa0/1 MAC C601.420F.0007` · EUI-64 แท้ต้องพลิกบิต U/L (C6→C4) แต่ชุดตัวเลือกไม่มีตัวที่ขึ้นต้น C401 มีแต่ตัวที่แทรก FFFE ถูกต้อง (C601:42FF:FE0F:7) จึงตอบ C — ตระกูลเดียวกับ #916/#1687
- **#103** ภาพ `ipconfig /all`: DNS Servers = **192.168.1.253** (คนละตัวกับ default gateway .100 และ DHCP server .254) เฉลย A ถูก
- **#109** ภาพให้ 10.88.31.64/26 · 10.19.63.80/28 · 10.23.98.128/27 → last usable = .126/.94/.158 พร้อมมาสก์ /26 /28 /27 ตามลำดับ เฉลย D ถูก (ตัวเลือกอื่นสลับมาสก์)
- **#123** ภาพ: SiteA `media type is SFP-SR` แต่ SiteB `SFP-LR` · reliability 255/255 ทั้งคู่ → ไม่ใช่ error แต่เป็น **transceiver ผิดชนิด** เฉลย D ถูก (ต่างจาก #75 ที่ media type เหมือนกันแต่ reliability 166/255)
- **#129** ภาพมีแค่ `R4 — 10.7.54.0/x` · 380 users +30% แล้ว → ต้อง /23 → broadcast 10.7.55.255 เฉลย D ถูก (C ใส่ broadcast ผิดเป็น .54.255)
- **#130** ภาพ: มี Gi0/0.2 (dot1Q 2) อยู่แล้วสำหรับ VLAN 2 · VLAN 3 ใช้ 10.10.2.1/23 → ต้องเพิ่ม subinterface dot1Q 3 กับมาสก์ 255.255.254.0 เฉลย B ถูก (เลขซับอินเทอร์เฟซไม่จำเป็นต้องตรงกับ VLAN)
- **#132** ตรวจครบ 4 ช่องจากผังภาพ: /30 last=.130 · /29 first=.9 · /28 last=.14 · /29 last=.14 · ตัวลวงคือ .129 (first), .131 (broadcast), 4.13/28 (ไม่ใช่ last) ถูกทั้งหมด
- **#135** ภาพ: `3612699 input errors, 3612699 CRC, 0 frame, 0 collisions, 0 late collision` → CRC ล้วนโดยไม่มี collision = **สายเสีย** ไม่ใช่ duplex mismatch เฉลย C ถูก
- **#143** ภาพ: Switch A `Speed 100 Duplex Half` · Switch B `Speed 10 Duplex Full` · ระยะ 99 m (ยังอยู่ในลิมิต Cat5) → พอร์ต down เพราะ **speed ไม่ตรงกัน** (duplex ต่างกันลิงก์จะยัง up) เฉลย A ถูก
- **#153 / #182** ภาพ `ipconfig /all` มี MAC สองตัว — ต้องเลือกของ **Wi-Fi adapter (B8-76-…)** ไม่ใช่ Wi-Fi Direct Virtual Adapter (1A-76-…) เฉลยถูกทั้งสองข้อ
- **#156** ภาพให้จำนวนโฮสต์ 923 / 225 / 3641 · บวก 10% = 1015 / 247 / 4005 → /22 (255.255.252.0) · /24 (255.255.255.0) · /20 (255.255.240.0) เฉลย C ถูก
- **#166** ภาพ 192.168.0.0/24 · VLAN 10 มี 3 PC และต้องรองรับอีก 30 VLAN → 32 ซับเน็ต = /29 = 255.255.255.248 เฉลย B ถูก
- **#173** ขยายภาพ 3 เท่าแล้วอ่านลายเส้น: Zone 3 และ Zone 4 เป็น **ลายตาราง (channel 11) เหมือนกันและวงทับกันจริง** ส่วน Zone 6 อยู่แถวล่างเยื้องออกไป ไม่ได้ทับกับ Zone 3 เฉลย C ถูก (ยืนยันการแก้ #1680 / V1 #429 ในรอบก่อน)
- **#181** ทุกตัว priority ดีฟอลต์ → เทียบ MAC: SW3 `0C:0E:15:1A…` < SW2 `0C:0E:15:22…` < SW1/SW4 `0C:E0…` เฉลย C ถูก
- **#184** ภาพเผย Bridge Priority: S2/S3 = 30000 (ต่ำสุด) เสมอกัน → เทียบ MAC BB < CC → **S2** เฉลย B ถูก
- **#186** ภาพ: Switch1 native VLAN 1 · Switch2 native VLAN 99 → **native VLAN mismatch** เฉลย D ถูก
- **#189** ภาพมีคอลัมน์ Device ID / Local Interface / Holdtime / Capability / Platform / Port ID = รูปแบบของ `show cdp neighbor` เฉลย B ถูก
- **#194** ภาพ: SW2 `vtp mode transparent` → ส่งต่อ VTP advertisement ที่รับมาทาง trunk โดยไม่ประมวลผล เฉลย D ถูก
- **#195** ภาพ `sh lacp neighbor` แสดง partner flags = **SP** (Slow, Passive) → ฝั่ง SW1 ต้องเป็น active เฉลย B ถูก
- **#202** ภาพมีแค่ `switchport voice vlan 50` ไม่มี `switchport access vlan` → ข้อมูลจาก PC ตกอยู่ใน VLAN 1 (ดีฟอลต์) เฉลย B ถูก
- **#203** ภาพ: พอร์ตเป็น `switchport mode trunk` และ `native vlan 5` (ค่า `access vlan 11` ไม่มีผลเมื่อพอร์ตเป็น trunk) → เฟรมไม่ติดแท็กเข้า **VLAN 5** เฉลย C ถูก
- **#213** ภาพ: SW1 native 10 · SW2 native 100 → VLAN 10 ออกจาก SW1 แบบไม่ติดแท็ก SW2 รับเข้าเป็น **VLAN 100** เฉลย B ถูก
- drag ที่เปิดภาพยืนยันแล้ว: #118 #131 #134 #136 #137 #139 #141 #146 #148 #150 #154 #155 #175 #176 #187 #207 — ตรงกับเฉลยทุกใบ

### 201–300 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 33 ใบ

- **#229 / #296** ภาพ `show spanning-tree vlan 30`: บรรทัด `protocol rstp` → **Rapid PVST+** และมี `Cost 19 / Port 1 (FastEthernet 2/1)` แปลว่าสวิตช์นี้ไม่ใช่ root และ Fa2/1 คือ **root port** เฉลย A,E ถูกทั้งสองข้อ
- **#237** ภาพมีทั้ง `enable password cisco123` และ `enable secret testing1234` → secret ชนะเสมอ เฉลย D ถูก
- **#242** MAC จากโจทย์: Switch3 `0C:0E:15:1D…` < Switch2 `0C:0E:15:22…` (เทียบไบต์ที่ 4: 1D < 22) เฉลย C ถูก
- **#248** ภาพตาราง MAC มีแค่ Gi1/0/1-3 · Sales-4 (Gi1/0/4) ยังไม่มีในตาราง → สวิตช์ **เรียน MAC ต้นทางของ Sales-4 ก่อน** แล้วส่งตรงไป Sales-1 ที่รู้จักอยู่แล้ว เฉลย B ถูก (ไม่ flood เพราะปลายทางรู้จัก)
- **#254** ภาพเป็นผังเฉย ๆ ค่า priority อยู่ในโจทย์: Branch-3 = **0** ต่ำสุด ชนะ `root primary` ของ Branch-4 (ซึ่งตั้งได้แค่ 24576) เฉลย C ถูก
- **#260** ภาพ: อินเทอร์เฟซมี `lldp transmit` / `lldp receive` แล้ว แต่ยังขาด `lldp run` ระดับ global เฉลย B ถูก
- **#263** ภาพ: PC A และ File Server อยู่ VLAN 13 แต่ trunk อนุญาตแค่ 10-12 → ต้อง `add 13` ไม่ใช่เขียนทับ เฉลย C ถูก
- **#264** ภาพ: พอร์ตมีทั้ง `spanning-tree portfast` และ `spanning-tree bpduguard enable` → รับ BPDU เมื่อไรเข้า **err-disable** ทันที เฉลย C ถูก
- **#266** ภาพ: SW1 native 999 · SW2 native 99 → trunk ยังขึ้น แต่ทั้งสอง VLAN ถูกรวมเป็น broadcast domain เดียว เฉลย B ถูก
- **#269** ภาพ GUI: `PSK` ติ๊ก Enable อยู่แล้ว (802.1X ไม่ได้ติ๊ก) → 802.11r ต้องเปิด Fast Transition แล้วเลือก **FT PSK** ให้ตรงกับ AKM เดิม เฉลย C ถูก
- **#274** ภาพผัง: PC2 (VLAN 14, SW4 Gi0/1) → PC7 (VLAN 14, SW9 Gi0/0) ผ่าน SW4 Gi0/2 → SW11 → SW9 · ต้องเป็น **trunk ที่ยอม 14 และ 108** ตลอดทาง เพราะ PC3/PC9 (VLAN 108) ใช้ลิงก์เดียวกัน เฉลย B ถูก
- **#275** ภาพ `show lldp entry` แสดง `Management Addresses IP: 10.5.110.2` → ต้องปิด TLV นี้ที่ต้นทาง (Cat9K-2) เฉลย D ถูก
- **#276** ภาพ: Fa0/2 ถูกตั้งเป็น `switchport mode trunk` + `trunk allowed vlan 3` ทั้งที่ PC2 อยู่ VLAN 2 → ต้องเปลี่ยนกลับเป็น access เฉลย C ถูก
- **#277** BID จากโจทย์: Switch 2 = 24586 ต่ำสุด เฉลย B ถูก
- **#279** ภาพ: ฝั่งแรกตั้ง `mode active` แต่ Po1 down → ฝั่งตรงข้ามเป็น static (mode on) ต้องเปลี่ยนเป็น **passive** จึงจะจับคู่กับ active ได้ เฉลย D ถูก
- **#282** ภาพ `show cdp`: `Sending CDP packets every 60 seconds` → ลด timer เป็น 10 ที่ Cat9300 เอง เฉลย B ถูก
- **#287** ภาพ: SW1 = `mode on` · SW2 = `mode active` → on ไม่คุยกับ active ต้องแก้ SW1 เป็น active/passive เฉลย B ถูก
- **#288** ต้องเห็น IP ของ R6 → ใช้ `sh cdp neighbor **detail**` (ตัวเลือก B ใช้ `sh cdp neighbor` เฉย ๆ ซึ่งไม่โชว์ IP) เฉลย D ถูก
- **#292** ภาพ: ทั้งสองฝั่ง `mode passive` → ไม่มีใครเริ่มเจรจา ต้องแก้ Switch2 เป็น active เฉลย C ถูก
- **#293** ภาพให้ตาราง MAC: PC1 = `00:50:79:66:68:00` ตรงกับ `0050.7966.6800` ในตัวเลือก C และใช้ `switchport mode access` ตามเงื่อนไข "ห้ามติดแท็กยกเว้น voice VLAN" เฉลย C ถูก
- **#299** ภาพเป็นผัง Ge0/0/0–0/0/15 · "respond but not initiate" = **passive** — ตัวเลือก D วางคำสั่งผิดโหมด (ควรอยู่ใต้ interface range) แต่เป็นตัวเลือกเดียวที่เป็น LACP passive จึงถูกในเชิงเฉลย (ปัญหาถ้อยคำต้นฉบับ ไม่แก้)
- **#261** drag Rapid PVST+ forwarding — ต้นฉบับให้ 6 ใบเลือก 4 และมีใบ "BPDUs received from the system module are processed and transmitted" ที่ก้ำกึ่ง คำอธิบายในระบบชี้แจงไว้แล้วว่าคำว่า *transmitted* เป็นของ listening/learning เฉลยเดิมป้องกันได้ ✔ ไม่แก้
- drag ที่เปิดภาพยืนยันแล้ว: #258 #272 #278 #284 #290 — ตรงกับเฉลยทุกใบ

### 301–400 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 41 ใบ

- **#306** ภาพ `sho vlan` ของ AccSw1 ระบุ **Gi1/1 = VLAN 11 (Staff)** → ฝั่ง AccSw2 ต้องเป็น access vlan 11 + Gi1/24 trunk เฉลย B ถูก
- **#307** ภาพ `show lldp`: ปัจจุบัน 30 วิ / holdtime 120 → ต้องการ 60 วิ และ refresh 180 วิ = `lldp timer 60` + `lldp holdtime 180` เฉลย B ถูก (C ใส่หน่วยเป็นนาทีผิด)
- **#309** ภาพ: Gi0/1 ทั้งสองฝั่งเป็น access ที่ยังไม่ระบุ VLAN (ตกไป VLAN 1) และ Sales = VLAN 500 → ต้องใส่ `switchport access vlan 500` เฉลย A ถูก
- **#310** ภาพ: SW1 f0/0 เป็น `access vlan 2` → NewSW ต้องเป็น trunk ที่มี **native vlan 2** (ให้เฟรมไม่ติดแท็กตกไป VLAN 2) และ allowed 2,10 เฉลย D ถูก
- **#312** ภาพ GUI: Layer 2 Security = WPA+WPA2, **PMF = Required**, ยังไม่ได้ติ๊ก policy ใด → ต้องเลือก WPA2 Policy + ปิด PMF + เปิด PSK เฉลย D ถูก
- **#317 / #320 / #323** ทั้งสามข้อเป็นตระกูล MAC table เดียวกันแต่ **ภาพต่างกัน** — #317/#320 ปลายทางมีในตาราง → forward ตรง (เฉลย C) ส่วน #323 ปลายทาง host D **ไม่มีในตาราง** → flood (เฉลย A) ถูกทุกข้อตาม exhibit ของตัวเอง
- **#322** ภาพระบุ priority VLAN10: SW2 = 24586 ต่ำสุด (primary) · ถ้า SW2 ล่ม ตัวถัดไปคือ **SW3 = 28682** เฉลย C ถูก
- **#326** ภาพระบุ VLAN 20: SW1 = 24596 ต่ำสุด เฉลย A ถูก (คนละชุดค่ากับ #322 — ตรวจแยกกัน)
- **#340** ภาพ GUI RADIUS: ช่อง **Network User ยังไม่ติ๊ก Enable** → ต้องเปิดเพื่อ auth ผู้ใช้ไร้สาย เฉลย C ถูก
- **#342** ภาพมี 3 เส้นทางถึง 172.16.32.x: /27 (EIGRP), /19 (OSPF), /24 (RIP) — 172.16.32.1 อยู่ใน /27 ด้วย → ชนะด้วย **longest prefix** ไม่ใช่ AD เฉลย A ถูก
- **#347 / #349** ตระกูลเดียวกัน แต่คนละภาพ — #347 ปลายทาง 192.168.12.16 ตกนอก OSPF /28 (0-15) จึงได้ RIP /27 · #349 ปลายทาง 192.168.10.16 ได้ RIP /27 เช่นกัน ถูกทั้งคู่
- **#350** ต้นฉบับบกพร่อง (OSPF /16 ก็เข้าตารางด้วย ทำให้มี 3 เส้นทางไม่ใช่ 2) — คำอธิบายในระบบระบุไว้แล้ว ตรงกับ V1 #62 / V2 #350 / V4 #1541 ✔ ไม่แก้
- **#351** 10.10.13.165 อยู่ใน **10.10.13.160/29** ซึ่ง next-hop 10.10.10.5 = ลิงก์ 10.10.10.4/30 → **Router 3** เฉลย B ถูก
- **#355 / #401** router-ID จาก `show ip interface brief` = loopback ที่สูงสุด (172.16.15.10 / 172.16.5.1) เฉลยถูกทั้งสองข้อ
- **#357 / #372 / #403** ทั้งสามข้อคือ hello/dead timer mismatch แต่ค่าต่างกันคนละภาพ (5 vs default 10 · 10 vs 20 · 5/20 vs 10/40) เฉลยถูกทุกข้อ
- **#362** ภาพเขียน `Hello 10, Dead 40` = ค่าดีฟอลต์ และ `Neighbor Count is 3` (ไม่ใช่ 6) เฉลย C ถูก
- **#366** ภาพ: 10.10.13.0/25 ปัจจุบันเป็น OSPF [110/2] ทาง Gi0/0 · eBGP AD 20 < 110 → เส้นทางย้ายไปเรียนผ่าน **Gi0/1** เฉลย C ถูก
- **#368** ภาพตาราง R2 มีแค่ 10.10.13.0/25 (0-127) ไม่มี /25 ตัวบน (128-255) → ส่งไม่ได้ เฉลย D ถูก
- **#380** ภาพ: `6692 subnets, **20 masks**` (→ B) และนับเส้นทางที่มีสองเน็กซ์ฮอปได้ **10 เส้น** รวม default D*EX (→ C) · AD 170 เป็นค่าดีฟอลต์ของ EIGRP external ไม่ได้ถูกแก้เอง เฉลย B,C ถูก
- **#386** ภาพ: 172.16.1.1 ไม่อยู่ใน 172.16.1.128/25 → ตกที่ default gateway **192.168.14.4** เฉลย A ถูก
- **#391** ภาพ: `auto-summary` เปิดอยู่ → 10.4.3.0 กลายเป็น 10.0.0.0 และ 172.16.4.0 กลายเป็น 172.16.0.0 ส่วน 192.168.2.0 เป็นคลาส C อยู่แล้ว เฉลย A,C,D ถูก
- **#394** ภาพ: 192.0.2.156 ไม่ตรงเส้นทางใด → default 192.168.4.1 ซึ่งอยู่บน **Serial2** เฉลย C ถูก
- **#398** ภาพ IP: wildcard 0.0.0.63 = 192.168.12.64–127 → เข้าเกณฑ์เฉพาะ Fa0/1(.65) S0/0(.121) S0/1.102(.125) เฉลย B,C,D ถูก
- **#400** ภาพ: EIGRP รู้จัก 192.168.25.16/.20/.24/.28 ทั้งหมดเป็น /30 → summary = **192.168.25.16/28** เฉลย C ถูก
- **#411 vs #412** สองข้อนี้ภาพคล้ายกันมากแต่ต่างกันจริง — #411 ปลายทาง 172.16.0.14 **ไม่ตรงเส้นทางใด** จึงตกที่ default (.246 Se0/1/0) ส่วน #412 ปลายทาง 172.16.3.14 ตรงทั้ง /24 และ /28 → **/28 ชนะด้วย longest prefix** (.254 Se0/0/1) เฉลยถูกทั้งสองข้อ
- **#413** ภาพ: R2 มี `ip route 10.1.1.0 … 192.168.0.1` (AD 1) และ network statement ของ OSPF บน R2 พิมพ์เป็น 192.168.1.2 (ผิดซับเน็ต) → static ชนะ เฉลย A ถูก
- **#414** ภาพ: มี host route `O 10.0.1.5/32 via 10.0.1.50` เฉลย D ถูก
- **#418** ภาพ: R1 `default-information originate` แต่ตาราง R1 ยัง `Gateway of last resort is not set` → ต้องสร้าง default route จริงก่อน เฉลย C ถูก
- **#419** ภาพ: 172.16.0.202 อยู่ใน **172.16.0.128/25 [110/38443]** (ไม่ใช่ /29 ที่ครอบแค่ 192-199) เฉลย C ถูก — คนละภาพกับ #903 (metric 32445)
- drag ที่เปิดภาพยืนยันแล้ว: #330 #336 #383 #416 — ตรงกับเฉลยทุกใบ

### 401–500 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 63 ใบ

- **#422 / #429 / #370 / #356** ตระกูล Atlanta–New York–Washington IPv6 — ภาพต่างกันเล็กน้อย (บางใบมีลิงก์ 2013::/126 เพิ่ม) ตรวจ next-hop ทีละใบ: #422 = B,D (2012::/126 ผ่าน 2023::2 · 2023::/126 ผ่าน 2012::2) · #429 = default ::/0 ออก Se0/0/0 · #370 = primary 2012::1 + floating 2023::3 AD 5 ถูกทุกข้อ
- **#425** 192.168.16.2 อยู่ในทั้ง /21 /24 /26 /27 → **/27 ยาวสุด** (IS-IS) เฉลย D ถูก — ชนะแม้ AD 115 จะสูงกว่า EIGRP 90
- **#426** ภาพ: มี `O*E2 0.0.0.0/0 [110/1]` อยู่แล้ว · static ใหม่ AD 120 > 110 → กลายเป็น floating เฉยอยู่ เฉลย D ถูก
- **#428** ภาพแสดง `S 209.165.200.224 [254/0] via 209.165.202.129` อยู่แล้ว → คำสั่งที่สร้างมันคือตัวเลือก A เฉลย A ถูก
- **#431** ภาพ: `O IA 192.168.10.32/28 [110/**193**]` (มี /27 [110/192] อยู่ข้าง ๆ เป็นตัวลวง) เฉลย E ถูก
- **#432** 10.0.1.15 อยู่ใน **10.0.1.0/28** (D, via 192.168.0.7) ซึ่งยาวกว่า /24 → เฉลย A ถูก
- **#438 / #445 / #470 / #484** ตระกูล MTU mismatch — #438 R1 ตั้ง `mtu 1600` ส่วน R2 ดีฟอลต์ 1500 · #445 ค้างที่ state **EXCHANGE** ซึ่งเป็นอาการเฉพาะของ MTU ไม่ตรง เฉลยถูกทุกข้อ
- **#439** 10.56.192.1 ไม่อยู่ใน /17 (0–127) และไม่อยู่ใน 10.56.128.0/18 (128–191) → ตกที่ default 10.56.0.1 เฉลย A ถูก
- **#440 / #444 / #449 / #450 / #452 / #465 / #477 / #493 / #495 / #497** ทั้งหมดเป็นโจทย์ longest-prefix จากตาราง route คนละใบ — คำนวณช่วงทีละเส้นแล้วตรงกับเฉลยทุกข้อ (เช่น #465 EIGRP [90/**144**] ที่ F0/11 ชนะ [90/1441] — ตระกูลเดียวกับ #1508 ที่แก้ไปแล้ว)
- **#451** ต้นฉบับมีจุดอ่อน (static /32 ย่อมชนะ OSPF /24 ด้วย longest prefix อยู่ดี) แต่ตัวเลือก A เป็นตัวเดียวที่ **ชี้ออก Gi0/0 ซึ่งเป็นทางเดียวที่ไปถึงเซิร์ฟเวอร์** และ AD 125 > 110 — คำอธิบายในระบบอธิบายไว้ครบแล้ว ✔ ไม่แก้
- **#454** ภาพ: R5 = DR, R4 = BDR, ทุกตัว Pri 1 → ต้องยก priority ให้ R3/R2 (255/240) · ตัวเลือก D ใช้ 259/256 ซึ่ง **เกินค่าสูงสุด 255** เฉลย C ถูก
- **#455** สองเส้นทางถึง 10.0.10.0/24: OSPF [110/10] กับ EIGRP [90/10] → **AD 90 ชนะ** → 10.0.1.5 เฉลย A ถูก
- **#457** ภาพ: ซับเน็ต WAN คือ **10.0.0.32/27** และ R1 ตั้ง `ip ospf priority 99` → R2 ต้องใช้ 10.0.0.34/27 + priority 100 เฉลย B ถูก
- **#462** ภาพ: เซิร์ฟเวอร์ใหม่ = 172.16.2.2 · ถ้าใส่ static เฉพาะ **/32 ไปที่ตัวเซิร์ฟเวอร์** เราเตอร์จะไม่มีเส้นทางไปยัง gateway 172.16.2.10 → ping gateway ไม่ได้ตามโจทย์ เฉลย C ถูก
- **#463** ภาพ: R3 = ทางหลักผ่าน g0/2 · R2 = สำรองผ่าน g0/1 → ต้อง AD > 1 เฉลย D (`g0/1 6`) ถูก
- **#467** ภาพ: `auto-cost reference-bandwidth 100000` → Gi0/0 (BW 10 Gbps) cost 10 · **Gi0/1 (BW 100 Gbps) cost 1** ⭐ · Gi0/2 cost 100 · Gi0/3 cost 1000 เฉลย B ถูก
- **#471** ภาพ: 192.168.1.0/24 มาจาก RIPv2 ฝั่ง Router A ถูก redistribute ที่ Router B เข้า OSPF Area 0 → Router D เห็นเป็น OSPF **AD 110** เฉลย A ถูก
- **#474 / #487 / #446 / #442 / #464** ตระกูล DR election — เปิดภาพยืนยัน DR/BDR ปัจจุบันทุกใบก่อนตอบ (เช่น #474 R3 = DR ที่ 10.10.10.3 · #487 R1 = DR ด้วย RID 192.168.2.8 สูงสุด) เฉลยถูกทุกข้อ
- **#478** ภาพ `show ipv6 route`: default ปัจจุบันเป็น `ND ::/0 [**2**/0]` → floating ต้อง AD > 2 เฉลย A (3) ถูก ไม่ใช่ 1 หรือ 2
- **#479** ภาพเผยว่า **OldR และ R2 ใช้ Router ID เดียวกัน (192.168.1.1)** → ต้องลบ router-id ที่ตั้งมือไว้ เฉลย D ถูก
- **#483** ภาพ: R1 `Hello 15, Dead 40` · R2 `Hello 15, Dead **45**` → ต้องแก้ dead-interval เป็น 40 (hello ตรงกันอยู่แล้ว) เฉลย C ถูก
- **#488** ภาพ: PC5 = **10.10.10.10** อยู่หลัง R3 (192.168.2.2) ส่วน PC2/3/4 อยู่หลัง R2 (192.168.2.3) → ต้องใช้ /24 ผ่าน R2 + host route /32 ผ่าน R3 เฉลย A,B ถูก
- **#496** ภาพ: R1 มี default AD 1 ผ่าน 172.16.20.2 (R3) และ floating AD 20 ผ่าน 10.0.0.2 → PC3 อยู่หลัง R3 เฉลย D ถูก
- **#498** ภาพ: สถานะที่ต้องการคือ `Pri **0** / FULL/**-**` = ไม่มี DR/BDR เลย ซึ่งเกิดจาก **network type point-to-point** ไม่ใช่แค่ตั้ง priority 0 (ซึ่งจะขึ้น FULL/DROTHER) เฉลย D ถูก
- drag ที่เปิดภาพยืนยันแล้ว: #480 — prefix length ทั้ง 4 ช่องตรงกับตาราง route ในภาพ

### 501–600 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 16 ใบ

- **#506** ภาพ: LAN ของ R3 = **10.0.15.0/24** และ R3 อยู่ที่ 10.0.20.3 บนวงร่วม เฉลย D ถูก (ตัวเลือก C ใส่มาสก์ /26 ผิด)
- **#510** ภาพ: `ip nat pool cisco 10.1.1.0 10.1.1.50 255.255.255.0` = สร้าง **พูลสำหรับ dynamic NAT** (ยังไม่มี `overload` จึงไม่ใช่ PAT) เฉลย C ถูก
- **#513** ภาพ dhcp pool มี network / domain-name / dns-server / netbios แต่ **ไม่มี `default-router`** → ออกนอกซับเน็ตไม่ได้ เฉลย D ถูก
- **#514** ภาพ `show ip dhcp conflict` แสดงทั้ง Ping และ Gratuitous ARP → ที่อยู่ที่ชนกัน **ถูกถอดออกจากพูลทั้งคู่** จนกว่าจะเคลียร์ เฉลย A ถูก
- **#530** ภาพ: R1 อยู่คนละซับเน็ตกับ DHCP server → R1 ต้อง `ip address dhcp` และ **R2 (เราเตอร์ที่ใกล้ไคลเอนต์)** ต้องมี `ip helper-address` เฉลย B,C ถูก
- **#532** ภาพ `show ip nat translations`: Inside global ของ 10.4.4.5 คือ **172.23.104.4** เฉลย D ถูก (172.23.103.10 เป็นฝั่ง outside)
- **#557** ภาพ: R2 มี Loopback0 172.17.0.1 · โหมด server อย่างเดียวโดยอิงนาฬิกาตัวเอง = `ntp master` เฉลย D ถูก
- **#565** ภาพ: VLAN 200 = **192.168.100.32/27** → wildcard 0.0.0.31 เฉลย A ถูก (B ใช้ /24 จะกิน VLAN 100 ไปด้วย)
- **#566** ภาพ: มี `ip domain-name` แล้ว แต่ยังไม่มีคีย์ RSA (`show crypto key mypubkey rsa` ว่างเปล่า) และ vty ยังไม่จำกัด transport → ต้อง `crypto key generate rsa 1024` + `transport input ssh` เฉลย A,C ถูก
- **#573** ภาพ: `Received on untrusted ports = 32` → พอร์ตที่ต่อ DHCP server ยังไม่ได้ trust เฉลย C ถูก
- **#575** ภาพ: `O IA 192.168.10.32/28 [110/**193**]` ครอบ .33 (ส่วน /27 [110/192] เป็นตัวลวง) เฉลย D ถูก — คู่แฝดกับ #431
- **#580** ภาพ: config มี ACL และ `ip nat inside source list 1 … overload` แต่ **ไม่มี `ip nat inside` / `ip nat outside` บนอินเทอร์เฟซ** เฉลย D ถูก
- **#582** ภาพ: โฮสต์อยู่ฝั่ง **Fa0/1 (LAN_INTERFACE)** และ ACL 100 ถูกแปะขาเข้าที่ Fa0/0 → ต้องเปิด UDP bootps จาก 10.0.1.1 มายัง 10.148.2.1 เฉลย D ถูก
- **#589** ภาพ: R1 = `ntp master 7` + `ntp server 209.165.200.225` (เป็นทั้ง server และ **client**) · R2 = server statement อย่างเดียว (client) · R3 = master + server (client ด้วย) · **R4 = master อย่างเดียว ไม่ใช่ client** เฉลย D (R1,R2,R3) ถูก
- **#590** ภาพ `show ip nat statistics`: บรรทัด **`Outside interfaces:` ว่างเปล่า** ส่วน Inside มี Ethernet0/1 แล้ว เฉลย D ถูก
- **#593** ภาพ `ipconfig /all`: **DNS Servers = 0.0.0.0** (แต่ default gateway/subnet mask ถูกต้อง) → เปิดชื่อเว็บไม่ได้ เฉลย B ถูก
- **#598** ภาพ: โทรศัพท์ IP อยู่ที่ SW1 ส่วน PC อยู่ที่ SW2 → หลักปฏิบัติคือ **trust มาร์กจากโทรศัพท์ที่ SW1 และ mark เอาเองที่ SW2** (PC ไม่น่าเชื่อถือ) เฉลย A ถูก
- drag ที่เปิดภาพยืนยันแล้ว: #523 #534 #545 #552 #583 #586 #588 #592 — ตรงกับเฉลยทุกใบ · #592 ต้นฉบับให้ 7 ใบเลือก 4 และมีใบ "provides local control … client-server scheme" ที่ก้ำกึ่ง แต่เฉลยมาตรฐานเลือก 4 ใบที่เจาะจง DHCP ที่สุด

### 601–620 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 9 ใบ

- **#605** ภาพ: มีแค่ `line vty 0 4` + `login` (ไม่มีรหัสผ่าน ไม่มี username) → ต้องสร้าง user **privilege 15** + `login local` จึงจะเข้า enable ได้ตรง ๆ เฉลย B ถูก
- **#607 / #609 / #610** สามข้อนี้ภาพ port-security คนละใบ ต้องอ่านค่าทีละบรรทัด
  - #607 Violation **Protect** → ทิ้งทราฟฟิกเงียบ ๆ ตัวนับ violation **ไม่ขยับ** ไม่มี syslog ไม่ err-disable เฉลย A,E ถูก
  - #609 `Aging Time 5 mins / Aging Type **Inactivity**` → ลบเมื่อไม่ใช้งาน 5 นาที และ `Sticky MAC Addresses : 2` → เรียนรู้เอง 2 ตัว เฉลย A,C ถูก
  - #610 `Configured 1 / Sticky 0 / Total 1` → MAC เดียวที่มีมาจากการตั้งมือ เฉลย B ถูก (ตัวเลือก A พูดถึง syslog ซึ่งจริงในโหมด shutdown แต่ exhibit ยัง `Violation Count : 0` — คำอธิบายในระบบชี้แจงไว้แล้ว)
- **#611** ภาพ: เปิด `ip arp inspection vlan 2` แต่พอร์ต Fa0/1 **ไม่ได้สั่ง `ip arp inspection trust`** → พอร์ตเป็น untrusted เฉลย C ถูก
- drag ที่เปิดภาพยืนยันแล้ว: #614 #617

### 621–700 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 20 ใบ

- **#621** ภาพ: ACL 101 บน R2 เขียน `deny tcp **10.0.20.0** … **10.0.10.0** …` แต่ทราฟฟิกที่ต้องบล็อกไหลจาก 10.0.10.0/26 ไป 10.0.20.0/26 → **ต้องสลับ src/dst** และเติม `permit ip any any` ท้ายลิสต์ (ตอนนี้ implicit deny กินทุกอย่าง) เฉลย A,D ถูก
- **#624** ภาพ: RTR-1 มี Loopback1-3 นอกเหนือจาก Gi0/0 → ถ้าใช้ `ip access-group` ที่ Gi0/0 จะกันได้แค่ปลายทางเดียว ต้องใช้ **`access-class` ที่ line vty** จึงครอบทุกอินเทอร์เฟซ · และพอร์ต SSH = 22 (ไม่ใช่ 23) เฉลย B ถูก
- **#628 vs #630** สองข้อนี้ภาพ DAI ต่างกันชัดเจน — #628 Fa0/1 มี **`ip arp inspection trust`** → ปลายทางต้องเป็นอุปกรณ์โครงสร้าง (เราเตอร์) · #630 Fa0/1 **ไม่มี trust** → เป็น untrusted จึงตรวจ ARP ทุกแพ็กเก็ต เฉลยถูกทั้งคู่
- **#629** ภาพ 4 สวิตช์: SW1 `no login local` + password ธรรมดา · SW2 ใช้ `password` (เก็บเป็น plain/Type 7) · SW4 ตั้งที่ **console** ไม่ใช่ vty · **SW3** ใช้ `secret` (hash) + `line vty 0 15` + `login local` ครบเงื่อนไข เฉลย C ถูก
- **#635** ภาพ: Web Server = 10.30.0.100 อยู่ VLAN 30 ส่วนผู้ใช้ที่ต้องบล็อกอยู่ VLAN 20 → ACL ต้องเรียง **deny ก่อน permit** และแปะขาเข้าที่ `int vlan 20` เฉลย D ถูก (A/B เรียง permit ไว้บนสุดทำให้ deny ไม่มีผล)
- **#641** ภาพ: บรรทัด `deny ip any 10.20.1.0 0.0.0.255` อยู่**ก่อน** `permit tcp … eq 22` จึงบล็อก SSH ทิ้ง → ต้องลบบรรทัด deny นั้น เฉลย D ถูก
- **#647** ภาพ ACL 101 มี `permit … eq telnet` อยู่แล้ว โจทย์ให้ "secure VTY" → ต้องเป็น `eq ssh` เฉลย D ถูก
- **#658** ภาพ: Gi0/0 = 10.100.100.0/24 · Gi0/1 = **192.168.0.0/16** → wildcard ต้องเป็น 0.0.255.255 และเลข ACL มาตรฐานต้องอยู่ในช่วง 1-99/1300-1999 (199 เป็นช่วง extended) เฉลย A ถูก
- **#668 vs #669 vs #699** สาม GUI คนละหน้า — #668 802.1X ติ๊กแล้วและ Fast Transition = **Adaptive** → ต้องตั้งเป็น Enable + ติ๊ก FT 802.1X · #669 PMF = **Disabled** → 802.11w ต้องตั้ง Required · #699 Layer 2 Security **ว่างเปล่า** → ต้องเลือก WPA+WPA2 แล้วติ๊ก 802.1X เฉลยถูกทุกข้อ
- **#665** drag port security — ยึดตามข้อสรุปเดิม (ช่อง 4 = `mac-address sticky`) ดูภาคผนวกท้ายไฟล์ ✔ ไม่แก้
- drag ที่เปิดภาพยืนยันแล้ว: #623 #651 #667 #676 #688 #689 — ตรงกับเฉลยทุกใบ

### 701–800 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 22 ใบ

- **#702** ภาพ: PC1 อยู่ VLAN 100 MAC 0000.abcd.0004 บน Gi1/0/15 · โจทย์ขอ "จำกัดได้สองที่อยู่" → `port-security` + `maximum 2` (ไม่ใช่ผูก MAC เจาะจง) เฉลย A ถูก
- **#703** ภาพ: SW1 มี `ip domain-name` + คีย์ RSA ถูกสร้างแล้ว + `login local` + username แล้ว แต่ยังขาด **`enable secret`** และ vty ยังเป็น `transport input telnet` เฉลย A,C ถูก (D ไม่จำเป็นเพราะคีย์มีอยู่แล้ว)
- **#754 vs #771** ภาพเดียวกัน (JSON 5 บรรทัด) แต่ถามคนละจุด — #754 ถามทั้งบล็อกบรรทัด 1-5 ที่ครอบด้วย `[ ]` = **array** · #771 ถามคำว่า `switch` ในบรรทัด 2 ซึ่งอยู่ซ้ายของ `:` = **key** ถูกทั้งคู่
- **#769** ภาพเป็น `{ }` เดียวที่มี 4 คีย์ (SW1-SW4) แต่ละคีย์เป็น array → **object เดียว** เฉลย A ถูก
- **#780** ภาพ: `{ }` หนึ่งชั้น มี 3 คีย์ แต่ละคีย์เป็น list → 1 object / 3 keys / 3 lists เฉลย C ถูก
- **#783** ภาพ: `"Routers": ["R1","R2","R3"]` → R1 และ SW1 เป็นสมาชิกใน array = **value** เฉลย B ถูก
- **#787** ภาพ aaaUser ซ้อนกันหลายชั้น นับ `{` ที่เป็น object ได้ **9 ตัว** (root, aaaUser, attributes, children-item, aaaUserDomain, attributes, children-item, aaaUserRole, attributes) เฉลย D ถูก
- **#772** ภาพ `{"Employee's name": "Arthur"}` = object เฉลย D ถูก
- **#790** ภาพแสดง `netconf ssh acl 1 / lock-time 100 / max-sessions 1 / max-message 10` — `max-sessions` มีช่วงที่ถูกต้อง 4-16 (ตั้ง 1 ไม่ได้) และตัวเลือกที่เหลือมีแค่ `max-message 1000` ที่เป็นการตั้งค่าใหม่ที่ถูกต้อง เฉลย B ถูก
- **#738** ภาพเป็นไวยากรณ์ `cisco_ospf_vrf { … ensure => 'present' }` = **manifest ของ Puppet** เฉลย D ถูก
- drag ที่เปิดภาพยืนยันแล้ว: #701 #715 #723 #725 #729 #732 #753 #757 #761 #766 #773 #776 #777 #778 #779 #784 — ตรงกับเฉลยทุกใบ

### 801–840 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 13 ใบ

- **#794** ภาพ `ifconfig`: `netmask **0xffffe000**` = 255.255.224.0 = **/19** · IP 10.8.138.14 ตกในบล็อก 128–159 → **10.8.128.0/19** เฉลย C ถูก
- **#808** LAB (ไม่คิดคะแนน) — แนบภาพผัง SW-1…SW-4 พร้อมตาราง VLAN 56/77 allowed และ 99 untagged เรียบร้อย
- **#811** ภาพหัวข้อถูก OCR เป็น "J" กับ "2J" ซึ่งคือ **3** และ **23** ช่อง → 2.4 GHz (b/g/n) = 3 ช่อง · 5 GHz (a/n) = 23 ช่อง เฉลยถูก
- **#829** ภาพ: `reliability 250/255` · `261028 input errors, 259429 CRC, 1599 frame` แต่ **0 collisions / 0 late collision** และ duplex เป็น Full → เป็น **error ทางกายภาพ** ไม่ใช่ duplex mismatch เฉลย C ถูก
- **#831** ภาพ: `15000 **collisions**, 179 deferred, 14278 CRC` บนลิงก์เราเตอร์–สวิตช์ → อาการคลาสสิกของ **duplex mismatch** (ต่างจาก #829 ที่ collision = 0) เฉลย B ถูก
- **#834** ภาพ: มี PC หลาย VLAN ทั้งสองฝั่ง → ต้องเป็น **trunk** และใช้โปรโตคอลมาตรฐาน = LACP → active/passive เฉลย C ถูก
- **#836** ภาพตาราง MAC มีเฉพาะ e0/1, e0/2, e0/3 — **host A บน e0/0 ไม่อยู่ในตาราง** → เฟรมจาก D ถูก flood เฉลย A ถูก
- **#837** ภาพ GUI: **Status ยังไม่ติ๊ก Enabled** และ Radio Policy = All → ต้องเปิด Status และตั้งเป็น **802.11g Only** (b คือรุ่นช้า, a คือ 5 GHz) เฉลย A,B ถูก
- **#840** ภาพ: Layer 2 Security = WPA+WPA2 อยู่ → Web Passthrough (Layer 3 Web Policy) ใช้ร่วมกับ L2 security ไม่ได้ ต้องตั้งเป็น **None** เฉลย D ถูก
- drag ที่เปิดภาพยืนยันแล้ว: #799 #801 #832

### 841–890 · ✅ ไม่มีข้อผิด — เปิดภาพแล้ว 20 ใบ

- **#842** ภาพแท็บ Advanced: **Static IP Tunneling ยังไม่ติ๊ก** → ต้องเปิดเพื่อให้ไคลเอนต์คง IP เดิมขณะโรม เฉลย A ถูก
- **#843** ภาพ: โทรศัพท์ **ไม่ใช่ยี่ห้อ Cisco** และสวิตช์เปิดแค่ `cdp run` → ต้องเปิด `lldp run` ระดับ global แล้ว enable ที่อินเทอร์เฟซที่ต้องการ เฉลย A ถูก
- **#844** ภาพ: R2 ต่อ R1 ทาง **g0/1** และต่อ R3 ทาง **g0/2** → ปิด CDP เฉพาะ g0/2 เฉลย C ถูก
- **#846** ภาพ `show interface g1/0/1 switchport`: `Native VLAN 321` · `Trunking VLANs Enabled: 100,200,300` · `Administrative Mode: trunk` → ฝั่งใหม่ต้องตั้งให้ตรงทุกค่า เฉลย A ถูก
- **#850** ภาพระบุ **SW4 priority 8192 / MAC …3b:40** เทียบกับ SW3 priority 32768 / MAC …3b:42 → SW4 ชนะ (ถ้อยคำในตัวเลือก A เขียน "priority is highest" กลับด้านกับความจริง แต่สวิตช์ที่ระบุถูก และตรงกับ V1 #716 — มีหมายเหตุกำกับในคำอธิบายแล้ว)
- **#859** ภาพ: SW1 Fa0/1 อนุญาต 100,200,**300** แต่ Fa0/2 อนุญาตแค่ 100,200 → ค่าคอนฟิกไม่ตรงกันจึงรวมเป็น bundle ไม่ได้ ต้อง `add 300` ที่ **Po1 ของ SW1** เฉลย C ถูก
- **#860** ภาพ: `Trunking VLANs Enabled: **1,22**` → ต้อง `add 23` มิฉะนั้น PC11/PC12 (VLAN 22) จะขาด เฉลย C ถูก
- **#862** ภาพ: **FlexConnect Local Switching ยังไม่ติ๊ก** → เปิดเพื่อให้ข้อมูลไคลเอนต์สลับที่ AP ไม่ต้องวิ่งกลับ WLC เฉลย B ถูก
- **#867** ภาพ Local Net Users: **ติ๊ก Guest User พร้อม Lifetime 86400** → ต้องเอาติ๊ก Guest User ออก บัญชีจึงไม่มีวันหมดอายุ (การใส่ 0 ในช่อง Lifetime ทำไม่ได้เพราะช่วงที่ยอมรับคือ 60–86400) เฉลย B ถูก
- **#872** ภาพ: `Broadcast SSID` ติ๊กอยู่ (ต้องเอาออกเพื่อ "enhanced SSID security") และ Radio Policy = All → ตั้งเป็น **802.11g only** (2.4 GHz ที่ทำได้ 54 Mbps) เฉลย C ถูก
- **#874** ภาพ: `Enable Session Timeout` ยังไม่ติ๊ก และ `Maximum Allowed Clients = 0` (ไม่จำกัด) → ตั้ง 3600 และ 10 ตามลำดับ (ช่อง *Per AP Radio* = 200 เป็นคนละค่า) เฉลย B,D ถูก
- **#875** ภาพ: PC4 VLAN 108 บน SW1 · PC1 VLAN 9 / PC2 VLAN 7 บน SW2 · R1 ต่อ SW1 Gi0/2 → ทุก trunk ต้องอนุญาต 5,7,9,108 และตั้ง **native vlan 5 ทั้งสองฝั่งของลิงก์ SW1-SW2** เฉลย C ถูก
- **#876** ภาพ: Router A ต่อ B ทาง **G0/0/0** และต่อ C ทาง **G0/0/1** → เปิด CDP ทั้งเครื่องแล้วปิดเฉพาะ G0/0/0 เฉลย B ถูก
- **#877** ภาพ: ซับเน็ตจริงคือ 10.0.0.252/30 (R1 = .253 บน Gi0/0 · ISP = **.254 บน Gi0/1**) → last usable = .254 อยู่ที่ **Gi0/1** เฉลย A ถูก
- **#880** ภาพ: `Wi-Fi Direct Clients Policy = Disabled` → ต้องตั้งเป็น **Not-Allow** จึงบล็อกอุปกรณ์ P2P ได้ (P2P Blocking Action เป็นคนละฟีเจอร์ ใช้กันไคลเอนต์คุยกันเอง) เฉลย A ถูก
- **#884** ทุกตัว priority ดีฟอลต์ → เทียบ MAC: MDF-DC-3 `08:0E:18:**1A**…` < MDF-DC-2 `08:0E:18:**22**…` เฉลย D ถูก
- **#889** ภาพ: SW2 `vtp mode transparent` → ส่งต่อ advertisement เฉย ๆ เฉลย D ถูก (ภาพเดียวกับ #194)
- drag ที่เปิดภาพยืนยันแล้ว: #852 #868 #871

### 891–940 · ✅ ถูกทุกข้อ — เปิดภาพ 27 ใบ
จุดที่ต้องคำนวณ: #894 metric สูงสุด 110/100 = 10.10.10.3 · #896 LAN R2 เป็น /27 + AD 111/112 · #904 /29 เหลือ 4 ที่ว่าง · #907 floating บน EIGRP external ต้อง AD > 170 → 171 · #914 longest match /11 (Router D) ชนะ /9 · #916 EUI-64 พลิกบิต 50→52 · #923 พลิกบิต 12→10 · #924 B* default มี + D EX + 10.90.0.0/16 Null0 → drop · #933 entry4 เป็น 255.255.**224.0** (/19) ไม่ใช่ /27 → /26 ยาวสุด · #937 10.10.10.16 หลุด /28 และไม่มี default → drop · #940 static AD 21 > eBGP 20

### 941–990 · ✅ ถูกทุกข้อ — เปิดภาพ 20 ใบ
จุดสำคัญ: #941 192.168.20.75 → /25 ยาวสุด (10.10.10.11) · #943 Router2 ไม่มี default route · #957 LAN คือ Fa0/1 และ ACL แปะที่ Fa0/0 · #962 helper ต้องชี้ 192.168.20.2 (ตัว DHCP server) ไม่ใช่ default-router · #965 `ip name-server` + `service password-encryption` ไม่จำเป็นสำหรับ SSH · #974 พิมพ์ `ip access-list 10 in` แทน `ip access-group` → ACL ไม่ถูกแปะ · #984 `enable secret level 15 0` = เก็บแบบ hash แต่พิมพ์เป็น plain text

### 991–1040 · ✅ ถูกทุกข้อ — เปิดภาพ 16 ใบ
จุดสำคัญ: #997 ACL มี seq 10/20/30/40(deny) → แทรกที่ 35 · #1006 "warning" อยู่ใน array `wheels` · #1015 JSON ขาด `}` ปิดท้าย · #1027 มี 3 array · #1030 EUI-64 พลิก CA→C8 · #1033 `port-channel min-links 1` · #1037 Full Duplex แต่มี 8.6 พันล้าน collisions → duplex ไม่เข้ากัน · #1039 พลิก 12→10

### 1041–1090 · ✅ ถูกทุกข้อ — เปิดภาพ 25 ใบ (รวม LAB 9 ข้อ: #1041 #1043 #1052 #1054 #1058 #1059 #1060 #1061 #1072 แนบภาพครบ)
จุดสำคัญ: #1045 load-balance = default สองเส้น (C) หรือ /1 สองเส้น (D) · #1046 exhibit เป็น **PAgP** จึงต้องเปลี่ยนเป็น LACP active · #1051 NewSwitch ใช้ **ISL** ต้องเปลี่ยนเป็น dot1q + add vlan 20 · #1053 `Po1(**R**U)` = Layer 3 → ต้อง `no switchport` · #1055 syslog "excessive collisions / Retry limit exceeded" = ทิ้งเฟรมหลัง 16 ครั้ง · #1056 exhibit เป็น static access + ISL → ต้องตั้ง trunk + dot1q · #1069 EIGRP [90/**6881**] F0/12 ชนะ [90/43618] · #1074 R4 priority 204 สูงสุด · #1080 MAC 0d:0E:18:**1B** < …**2A**

### 1091–1140 · ✅ ถูกทุกข้อ — เปิดภาพ 24 ใบ
ตระกูล `show interface` 4 ข้อภาพคนละใบ: #1095 rate 0/txload 1 → queueing (ตัดตัวเลือกอื่นหมด) · #1099 txload 255/255 rate 200 Mbps → high throughput · #1117 rate 234 Mbps → high throughput · #1131 **Half-duplex + 119 collisions** → collisions ✔ ถูกทุกข้อตาม exhibit ของตัวเอง
root bridge: #1120 SW4 (28672, 0b:09) · #1123 SW3 (32768, 01:1c) · #1136 SW4 (8192, 05:4a) — เทียบ priority ก่อนแล้วค่อย MAC ถูกทุกข้อ
อื่นๆ: #1111 ทั้งสองฝั่งเป็น dynamic auto → ต้องบังคับ trunk · #1121 OSPF 9443 (G0/20) ชนะ · #1124 gateway 10.2.2.10 อยู่นอก /29 (0-7) · #1133 helper ต้องอยู่ที่ Gi0/0 ฝั่งไคลเอนต์ · #1139 EIGRP 2170 (G0/12) ชนะ · #1093 ต้นฉบับบกพร่อง — คงเดิมตามที่บันทึกไว้

### 1141–1190 · ✅ ถูกทุกข้อ — เปิดภาพ 25 ใบ
#1145 EIGRP 10259 (G0/24) · #1149 exhibit `sho vlan` ระบุ Gig1/3-1/4 = VLAN 10 (Servers) และ Server1 = 10.0.2.22 · #1151 DHCP Server = 192.168.25.100 (คนละตัวกับ gateway .1 และ DNS .254) · #1164 broadcast 3553 จาก 7331 (~48%) → broadcast storm · #1174 172.16.32.8 ตกใน /26 (ไม่ใช่ /30 ที่ครอบแค่ 4-7) · #1176 EUI-64 พลิก 13→11 · #1177 Half-duplex + 831 collisions + 383 CRC → interface config · #1189 txload 255/255 · root bridge #1152 SW2(00:ac) #1172 SW3(02:f8) #1187 SW2(04:1e)

### 1191–1240 · ✅ ถูกทุกข้อ — เปิดภาพ 18 ใบ
#1196 SW1 เป็น passive ทั้งคู่ → SW2 ต้อง active · #1197 Half Duplex 1000Mbps + 50 collisions + protocol down · #1198 ครอบ 10.1.40.0–10.1.41.231 ต้องใช้ /23 · #1204 SW_2 เป็น dynamic auto → SW_1 ต้อง desirable · #1209 172.16.3.254 อยู่ใน C 172.16.2.0/23 · #1212 DNS ที่ตั้งไว้ (.254) ไม่ใช่ DNS จริง (196.1.38.132) · #1215 EUI-64 พลิก 12→10 · #1221 longest /11 → Router D · #1223 /28 ครอบ .3 (ไม่ใช่ /22 ที่ครอบแค่ 10.10.0-3.255) · #1227 เราเตอร์ /27 แต่ PC ตั้ง /26 · #1240 PC5=10.10.10.10 อยู่หลัง R3 (192.168.2.2)

### 1241–1290 · ✅ ถูกทุกข้อ — เปิดภาพ 15 ใบ
#1241 ปลายทาง 172.18.32.38 ตกใน C 172.18.32.36/30 (.36–.39) → Gi0/0 ยาวสุด · #1245 1790 CRC/frame แต่ collision = 1 → bad checksum (ไม่ใช่ broadcast/queue) · #1254 "R20" เป็น value ของคีย์ "router" · #1259 Total output drops 100 + input queue 175/300 แต่ error 0 → queueing · #1261 บรรทัด 3 เป็น `{...}` → object · #1269 EUI-64 MAC 13-be-bf-b9-00-01 → พลิก 13→11 = 11BE:BFFF:FEB9:1 · #1274 10.225.34.225 ตกใน B 10.224.0.0/**15** (ครอบ 10.224–10.225) ยาวกว่า /11 และ /9 → Router C · #1276 OSPF 6906 ชนะ 23018 (RIP AD 120 แพ้) → G0/6 · #1279 สถานะ **notconnect** (ไม่ใช่ disabled/err-disabled) → สายผิดชนิด · #1281 MAC ca-01-b3-b8-00-01 → C801:B3FF:FEB8:1 · #1242 ภาพ WLC: Allow AAA Override และ Client Band Select ยังไม่ติ๊กทั้งคู่ → เฉลย A,C ถูก
root bridge #1255 · #1273 · ภาพเป็นผังเปล่า ไม่มี priority ในรูป ค่าอยู่ในตัวเลือก — #1255 SW4 (12288, 03:42) ชนะ SW1 (12288, 95:2b) · #1273 SW4 (53248, 22:11) ชนะ SW3 (53248, 35:d9)
drag เปิดภาพยืนยันครบ: #1256 #1263 #1267 #1271 #1272 #1275 #1277 #1282 #1284 #1289 · LAB แนบภาพครบ: #1258 #1260 #1266 #1285

### 1291–1340 · ⚠️ พบข้อบกพร่องของต้นฉบับ 1 ข้อ (#1331) — เปิดภาพ 20 ใบ
- **#1331** ⚠️ MAC ในรูปคือ `ca-01-b6-b4-00-01` ค่าที่ถูกต้องตาม EUI-64 คือ **C801:B6FF:FEB4:1** แต่ตัวเลือก D ในชุดนี้พิมพ์เป็น `FEB8` — เทียบกับ **#1352** ซึ่งเป็นข้อเดียวกัน (รูปเดียวกัน ค่าเดียวกัน) และมีตัวเลือก `FEB4` ที่ถูกต้อง → **คงเฉลย D ไว้** เพราะเป็นตัวเลือกเดียวที่มีโครงสร้าง `C801:B6FF:FE..` ถูกต้อง และเพิ่มบรรทัด ⚠️ หมายเหตุ อธิบายข้อบกพร่องไว้ในคำอธิบายแล้ว (ไม่แก้ข้อความตัวเลือกตามกฎ)
- #1292 drag เปิดทั้งสองรูป: 172.16.2.0/24 [120/2]→RIP · 192.168.1.0/24 [110/84437]→OSPF · 192.168.2.0/24 [90/3184437]→EIGRP · 207.165.200.244/30 [1/1]→static · 207.165.200.248/30 เป็น directly connected จึงเป็นตัวลวง ✔
- #1296 txload/rxload 255/255 + rate 230/200 Mbps + error 0 → high throughput (ต่างจาก #1259 ที่ rate = 0) · #1297 OSPF 1912 ชนะ 28968 → F0/7 · #1303 MAC 13-b0-ab-cc-00-01 → 11B0:ABFF:FECC:1 · #1311 MAC 13-d8-bf-69-00-01 → 11D8:BFFF:FE69:1 · #1313 ทั้งก้อนครอบด้วย `{}` → object (ข้างในมี array ซ้อน) · #1318 R1 Hello **10** / R2 Hello **15** → ต้องตั้ง hello-interval 10 ที่ R2 (dead 40 ตรงกันอยู่แล้ว) · #1320 MAC ca-01-be-4a-00-01 → C801:BEFF:FE4A:1 · #1333 EIGRP 6451 ชนะ 52201 → F0/2 · #1339 ลิงก์ 3 เส้น ถ้าเหลือ 1 เส้นจะรับ 1800 Mbps ไม่ไหว → `port-channel min-links 2`
- root bridge #1294 (ภาพเดียวกับ #1255 ทุกบิต) SW2 (20480, 54:4e) ชนะ SW1 (20480, 71:e3) · #1305 SW1 (8192, 30:eb) ชนะ SW2 (8192, 80:fb)

### 1341–1390 · ✅ ถูกทุกข้อ — เปิดภาพ 24 ใบ
**ตระกูล root bridge ที่ priority อยู่ในรูปเท่านั้น — เปิดครบทั้ง 6 ใบ**
- #1364 SW2/SW3 = 4096 (SW1/SW4 = 8192) → MAC SW2 `0C:0E` < SW3 `0C:E0` → **SW2** ✔
- #1365 SW3/SW4 = 4096 → SW4 `0C:0A:05` < SW3 `0C:0A:18` → **SW4** ✔
- #1375 SW3/SW4 = 8192 (SW1/SW2 = 32768) → SW4 `0C:0A:18` < SW3 `0C:0A:A8` → **SW4** ✔
- #1383 SW2/SW3 = 8192 → SW3 `0C:0A:18` < SW2 `0C:0A:A8` → **SW3** ✔
- #1387 SW2 = **4096** ต่ำสุดเดี่ยว ๆ (SW3 8192 · SW4 12288 · SW1 32768) → **SW2** ✔ (ไม่ต้องเทียบ MAC)
- #1388 SW3/SW4 = 4096 → SW3 `0C:0B:15` < SW4 `0C:B0:18` → **SW3** ✔

จุดอื่นที่ต้องคำนวณ: #1341 PC1 อยู่ G0/0 → ARP ถูก flood ออกทุกพอร์ตยกเว้น G0/0 · #1342 Half-duplex + 480 collisions + 35 runts → duplex mismatch · #1346 MAC ca-01-b8-b8-00-01 → C801:B8FF:FEB8:1 · #1352 MAC ca-01-b6-b4-00-01 → C801:B6FF:**FEB4**:1 (ตัวเลือกถูกต้อง ต่างจาก #1331) · #1353 192.168.10.33 ตกใน O IA 192.168.10.32/28 → metric **193** (ไม่ใช่ /27 ที่ metric 192) · #1354 MAC ca-01-b8-b3-00-01 → C801:B8FF:FEB3:1 · #1355 LAN R2 = wildcard 0.0.0.7 → **/29 = 255.255.255.248** และ next-hop สำรอง = 10.0.0.**6** AD 91 > EIGRP 90 · #1359 ตาราง Data 400 / VoIP 4041 บนพอร์ตโทรศัพท์+PC → access vlan 400 + voice vlan 4041 · #1360 /32 ยาวสุด → OSPF via 192.168.1.1 · #1366 R2 มี 10.22.1.0/24 กับ 10.40.1.0/30 ต่อตรงอยู่แล้ว จึงเรียนรู้เฉพาะอีก 4 เส้น · #1367 รายงาน Full Duplex แต่มี 100 collisions + 100 CRC + 100 frame → ฝั่งตรงข้ามเป็น half duplex · #1374 Host C ตั้ง gateway เป็น 192.168.1.1 ทั้งที่ Router A คือ 192.168.1.**254** · #1381 Total output drops **135,298,429** แต่ CRC 4 / collision 11 → port oversubscription
WLC ที่เปิดภาพ: #1372 PSK ติ๊กแล้ว แต่ WPA2 Policy และ MAC Filtering ยังไม่ติ๊ก → C,D · #1378 WPA2 Policy ติ๊กแล้ว เหลือ AES(CCMP128) + PSK → B,D · #1385 ยังว่างทั้งหมด → WPA2 Policy+AES และ PSK/ASCII+คีย์ → B,D
LAB แนบภาพครบ: #1344 #1348 #1350 #1351 #1357 #1358 #1361 #1362 #1369 #1373 #1376 #1377

### 1391–1440 · ✅ ถูกทุกข้อ — เปิดภาพ 16 ใบ (ช่วงนี้ใช้ exhibit ร่วมกับคลัง V3)
#1396 ปลายทาง 10.1.1.19 ตกใน RIP 10.1.1.16/**28** (ครอบ .16–.31) ซึ่งยาวที่สุด แม้ AD 120 จะสูงสุด → F0/0 · #1397 LAN ของ R3 = 10.0.15.0/24 และ R3 บนลิงก์กลางคือ 10.0.20.**3** → `ip route 10.0.15.0 255.255.255.0 10.0.20.3` · #1398 drag: /25→255.255.255.128 · /28→255.255.255.240 · /29→255.255.255.248 · /27→255.255.255.224 · 255.255.255.252 เป็นตัวลวง ✔ · #1399 R2 ต่อ R3 ทาง **g0/2** → `no cdp enable` เฉพาะพอร์ตนั้น (ไม่ใช่ `no cdp run` ที่ตัดกับ R1 ด้วย) · #1401 R1 ต่อ SW1 Gi0/2 · PC4 VLAN 108 บน SW1 · PC1 VLAN 9 / PC2 VLAN 7 บน SW2 → trunk ทุกเส้นต้องอนุญาต 5,7,9,108 และ native vlan 5 ทั้งสองฝั่งของลิงก์ SW1–SW2 · #1403 ต้องประหยัดที่สุดแต่เหลือว่างอีก ≥2 → /30 เหลือ 0 จึงต้องใช้ **/29** (6 usable, ใช้ 2 เหลือ 4) · #1405 ภาพ WLC: MAC Filtering **ติ๊กอยู่** และ Layer 2 = WPA+WPA2 → ต้องเอาติ๊กออกและตั้ง Layer 2 Security = None ก่อนจึงเปิด Web Auth ได้ · #1413 เราเตอร์เป็น /27 แต่ PC ตั้ง 255.255.255.**192** (/26) → Subnet Mask · #1416 มีวงเล็บปีกกานอกสุดชุดเดียว → 1 object · #1418 SW1 f0/0 เป็น access vlan 2 (สวิตช์ยี่ห้ออื่น ไม่รองรับ trunk) → NewSW ต้อง trunk allowed 2,10 + native vlan 2 · #1423 drag: R1 ได้ 1 คำสั่ง · R2 ได้ 2 · R3 ได้ 1 และ next-hop สอดคล้องกันทุกเส้น (R2=.1/.6 · R1=.2 · R3=.5) ส่วน host route /32 สองบรรทัดเป็นตัวลวง · #1432 PC1 อยู่ฝั่ง f1/0 = inside · f0/0 = outside → `ip nat inside source static` + R2 ต้องมี route กลับไปหา 10.199.77.100 · #1437 Speed **100 vs 10** → ลิงก์ไม่ขึ้น (duplex mismatch ลิงก์จะยัง up)

### 1441–1490 · ✅ ถูกทุกข้อ — เปิดภาพ 15 ใบ
#1441 ชั้นที่ใหญ่สุด 29 เครื่อง → ต้อง /27 (30 usable) × 4 ชั้น = **/25** เป็น summary · #1449 ULA = FC00::/7 → 1111110x → **11111100** · #1455 drag คำนวณครบ: /30 last usable = 209.165.202.130 · /29 first usable = 192.168.4.9 · /28 last usable = 192.168.3.14 · /29 last usable = 192.168.3.14 (คนละบล็อกกัน) · #1461 PC2 (VLAN 14) → SW4 Gi0/2 → SW11 → SW9 Gi0/2 → PC7 และต้องคง VLAN 108 ของ PC3/PC9 ไว้ จึง trunk 14,108 · #1463 SW1 = mode **on** ส่วน SW2 = active → ต้องเปลี่ยน SW1 เป็น active/passive · #1464 R5 Gi0/1 ออกอินเทอร์เน็ต Gi0/2 ไป R6 → `no cdp enable` ที่ Gi0/1 + `no lldp run` ทั้งเครื่อง + `cdp run` (ข้อ A ใช้ `no cdp run` ซึ่งจะตัด Gi0/2 ด้วย) · #1469 G0/0 = 10.100.100.0/**24** → wildcard 0.0.0.255 · G0/1 = 192.168.0.0/**16** → 0.0.255.255 และ standard ACL ต้องเป็นเลข 1–99 · #1473 ACL 10 มีแต่ deny → implicit deny ปิด PC-2 ด้วย ต้องเติม `permit any` · #1475 PC1 อยู่ G0/0 → flood ทุกพอร์ตยกเว้น G0/0 · #1490 R4 = AS 65513 · R2 = AS 65512 → **eBGP AD 20** ต่ำสุด
root bridge: #1456/#1468 ภาพไม่มี priority (ดีฟอลต์ทั้งหมด) เทียบ MAC ล้วน → #1456 SW3 (0C:0E:15:**1A**) ชนะ SW2 (…**22**) · #1468 Switch3 (0C:0E:15:**1D**) ชนะ Switch2 (…**22**) · #1462/#1466/#1478 มี BID ครบ → Switch2 = **24586** ต่ำสุดทั้งสามข้อ

### 1491–1540 · ✅ ถูกทุกข้อ — เปิดภาพ 27 ใบ
**ตระกูล longest-match ที่ต้องเปิดตารางเราต์จริง**
- #1495 172.16.1.15 → D 172.16.1.0/**28** [90/10] via 192.168.7.**7** (ยาวกว่า /24 connected และ /16 loopback) ✔
- #1497 172.16.3.14 → O 172.16.3.0/**28** via 207.165.200.**254** Se0/0/1 (ชนะ S 172.16.3.0/24) ✔
- #1499 10.0.5.5 ไม่ตรงซับเน็ตใด (มีแค่ 10.0.2/3/4) → ใช้ default 192.168.4.1 ซึ่งอยู่บน **Serial2** ✔
- #1506 192.168.16.2 → /**27** ยาวสุด (แม้ AD 115 ของ IS-IS จะสูงกว่า EIGRP 90) ✔
- #1510 192.168.0.100 → S 192.168.0.0/**24** via 10.0.12.2 ซึ่งอยู่บน **Gi0/1** ✔
- #1511 10.10.13.214 → 10.10.13.**208**/29 (ครอบ .208–.215) ✔
- #1505 172.16.0.202 → /**25** (128–255) metric **38443** — /29 ครอบแค่ .192–.199 จึงไม่ตรง ✔ (คนละใบกับ #419/#903)
อื่น ๆ: #1493 มี 4 คีย์ SW1–SW4 · #1496 ลิงก์รองคือ .5/.6 → floating static AD 10 ชี้ 10.10.10.6 / 10.10.10.5 · #1500 R1 area **0** vs R2 area **3** → area mismatch · #1502 R1 Hello 5/Dead 20 vs R2 Hello 10/Dead 40 → timer mismatch · #1503 `D` = EIGRP internal (AD 90) · #1504 R1 มี `default-information originate` แต่ยังไม่มี default route ของตัวเอง → ต้อง `ip route 0.0.0.0 0.0.0.0 10.10.10.18` · #1507 `D 192.168.2.0/24 [90/…]` = internal (ส่วน EX คือ external) · #1509 `O 192.168.1.0/24` · #1514 `ip nat pool` = dynamic NAT pool · #1515 IP ที่ conflict ถูกถอดออกจาก pool จนกว่าจะแก้ · #1519 Violation Mode = **Protect** → ทิ้งทราฟฟิก แต่ counter ไม่เพิ่มและไม่มี syslog · #1526 R1 รู้จักแค่ 10.10.10.0/24 → static ไป 20.20.20.0 ผ่าน 10.10.10.2 · #1529 192.168.16.143/28 → บล็อก .128–.143 จึงเป็น broadcast · #1533 /30 เป็นมาสก์ที่เหมาะกับลิงก์ระหว่างเราเตอร์ และ 209.165.201.2 เป็น usable (10.2.1.3/30 เป็น broadcast) · #1534 source หลัง NAT = inside global = 172.23.104.4 · #1535 ACL สลับ source/destination และไม่มี permit ท้าย · #1536 10.10.13.10 ตกใน O 10.10.13.0/25 = network route · #1539 Switch1 native VLAN 1 vs Switch2 native VLAN 99 → native VLAN mismatch
#1508 · #1522 คงตามที่ตัดสินไว้เดิม (ดูตาราง "ข้อที่แก้ไข" ด้านบน)
