# CCNA 200-301 — Version 2 (ข้อ 1–808) : รายงานผลการตรวจสอบและแก้ไข

> ตรวจทานทั้ง **808 ข้อ** ใน `src/data/questionsV2.ts` ทีละข้อ
> เกณฑ์ = หลัก CCNA 200-301 ที่ถูกต้องจริง (ไม่ยึดตามเฉลยในเอกสารต้นฉบับ)
> ทุกข้อที่แก้ มี `⚠️ หมายเหตุ (แก้เฉลย)` หรือ `⚠️ หมายเหตุ (แก้โจทย์)` กำกับอยู่ในคำอธิบายของแอปแล้ว

---

## สรุปผล

| รายการ | จำนวน |
|:---|---:|
| ข้อทั้งหมดที่ตรวจ | 808 |
| **เฉลยผิด → แก้แล้ว** | **8** |
| **โจทย์/ตัวเลือกผิด → แก้แล้ว** | **4** |
| คำอธิบายขัดกับเฉลย → แก้แล้ว | 3 |
| ผ่านการตรวจ ไม่ต้องแก้ | 793 |

---

## 1. ข้อที่ **เฉลยผิด** (แก้คำตอบแล้ว)

| ข้อ | เดิมตอบ | **ที่ถูกต้อง** | หัวข้อ |
|----:|:---:|:---:|:---|
| 176 | สลับกลุ่ม | **(drag)** | Wi-Fi terms — ESS ↔ Distribution System |
| 261 | สลับ 2 ชิ้น | **(drag)** | Rapid PVST+ forwarding state |
| 267 | D,E | **A,E** | subnetting — first usable address ของ /23 |
| 274 | C | **B** | trunk allowed VLAN ระหว่างสวิตช์ |
| 283 | C | **B** | LAG บน WLC |
| 727 | B | **A** | physical access control |
| 730 | สลับ 2 ชิ้น | **(drag)** | ลำดับคำสั่ง port security |
| 751 | B,C | **A,C** | คำสั่งที่ต้องเพิ่มเพื่อ SSH remote config |

---

## 2. ข้อที่ **โจทย์หรือตัวเลือกผิด** (แก้ข้อความแล้ว)

| ข้อ | ปัญหาที่พบ | สิ่งที่แก้ |
|----:|:---|:---|
| 137 | IPv6 multicast พิมพ์ตก `::` → `ff05:1:3` | แก้เป็น `ff05::1:3` |
| 156 | ตัวเลือก A กับ D ซ้ำกันทุกตัวอักษร | คืนตัวเลือก D เป็นชุด /21, /23, /21 |
| 198 | ตัวเลือก B เขียน "disabling TCP" | แก้เป็น "disabling TPC" (Transmit Power Control) |
| 261 / 499 / 501 / 673 / 720 / 730 | โจทย์บอก "Not all … are used" แต่ไม่มีกลุ่มตัวลวงในแอป | เพิ่มกลุ่ม `ไม่ใช้ (ตัวลวง)` ให้ครบตามภาพ exhibit |

---

## 3. ข้อที่ **คำอธิบายขัดกับเฉลย** (เฉลยถูกอยู่แล้ว แต่เนื้อคำอธิบายเชียร์คำตอบเก่า)

| ข้อ | คำอธิบายเดิมเชียร์ | เฉลยจริง |
|----:|:---:|:---:|
| 420 | B | **C** |
| 446 | A | **D** |
| 467 | B | **D** |

---

## 4. ผลพลอยได้ — เจอ Version 1 ผิดเพิ่มจากการเทียบข้ามชุด

การเทียบโจทย์ที่ซ้ำกันระหว่าง V1 กับ V2 ทำให้พบว่า **Version 1 ยังผิดอีก 4 ข้อ** และแก้เรียบร้อยแล้ว:

| V1 ข้อ | เดิมตอบ | **ที่ถูกต้อง** | เหตุผลย่อ |
|----:|:---:|:---:|:---|
| 236 | (drag) | **(drag)** | หน้าที่ DHCP — ใช้ "offers DNS configuration" แทน "reduces administrative burden" + เพิ่มตัวลวงเป็น 3 ชิ้น |
| 655 | D | **A** | host route /32 ควรใช้ AD 115 (ตรงกับ V2 ข้อ 602) |
| 750 | A,E | **A,B** | /29 ครอบแค่ .0–.7 ไม่ถึง PC5 ที่เป็น .10 |
| 1026 | C | **D** | เหมือนข้อ 655 |

---

## 5. การตรวจอัตโนมัติ (test case)

สคริปต์ตรวจโครงสร้างทั้งสองไฟล์ ผลลัพธ์ **ผ่านทุกข้อ**:

| การตรวจ | V1 (1242) | V2 (808) |
|:---|:---:|:---:|
| id ครบ ไม่ซ้ำ ไม่ขาด | ✅ | ✅ |
| ทุก key ใน `correct` มีอยู่จริงในตัวเลือก | ✅ | ✅ |
| ลำดับตัวเลือก A→B→C→D ต่อเนื่อง | ✅ | ✅ |
| `single` = 1 คำตอบ / `multi` ≥ 2 | ✅ | ✅ |
| จำนวนคำตอบตรงกับ "Choose two/three" | ✅ | ✅ |
| ทุกข้อมีตัวเลือกครบ 4 ข้อขึ้นไป | ✅ | ✅ |
| ตัวอักษรใน `✅ ทำไม X ถูก` ตรงกับเฉลยจริง | ✅ | ✅ |
| `❌ ทำไมข้ออื่นผิด` ไม่ระบุตัวเลือกที่เป็นคำตอบ | ✅ | ✅ |
| drag ไม่มี item ซ้ำ / ไม่มีหมวดว่าง | ✅ | ✅ |
| โจทย์ซ้ำกัน เฉลยตรงกันทุกกลุ่ม | ✅ 37 กลุ่ม | ✅ 21 กลุ่ม |
| TypeScript typecheck + build | ✅ | ✅ |

**ทดสอบในเบราว์เซอร์จริง (Chromium):** ทุกข้อที่แก้ในทั้งสองชุด กดตอบตามเฉลยใหม่แล้วขึ้น "✅ ถูกต้อง!" ทุกข้อ · ตัวเลือกเรียง A→D · drag วางครบไม่มีชิ้นผิด · 0 page error · ธีมขาวยังคงอยู่แม้ OS เป็น dark mode

---

*อัปเดตล่าสุด: 5 สิงหาคม 2026*
