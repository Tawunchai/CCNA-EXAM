# ผลตรวจสอบเฉลย — Version 2 (808 ข้อ) รอบตรวจซ้ำ

> วันที่ตรวจ: 11 สิงหาคม 2026 · ไฟล์: `src/data/questionsV2.ts`
> เกณฑ์ = หลัก CCNA 200-301 ที่ถูกต้องจริง (ไม่ยึดตัวอักษรจากเฉลย dump ตรง ๆ)
> **โจทย์และตัวเลือกทั้งหมดคงไว้ตามไฟล์เดิมทุกตัวอักษร ไม่มีการเขียนโจทย์ใหม่** — แก้เฉพาะ `correct` และคำอธิบายของข้อที่เฉลยผิด

---

## สรุปผล

| รายการ | จำนวน |
|:---|---:|
| ข้อทั้งหมดที่ตรวจ | **808** |
| — ข้อเลือกตอบ (single / multi) | 682 |
| — drag & drop | 126 |
| 🔴 **เฉลยผิด → แก้แล้ว** | **1** |
| 🟡 โจทย์ต้นฉบับบกพร่อง/กำกวม (เฉลยคงเดิม แต่ควรรู้) | 6 |
| ✅ ผ่านการตรวจ ไม่ต้องแก้ | 807 |

**🔴 ข้อที่ต้องแก้ = ข้อ 283 เพียงข้อเดียว (แก้ให้แล้ว)**

---

## 1) 🔴 ข้อที่เฉลยผิด — แก้แล้ว

### ข้อ 283 — LAG บน Cisco WLC

**โจทย์ (คงเดิม):** What is a requirement when configuring or removing LAG on a WLC?

| | ตัวเลือก | |
|:--|:--|:--|
| A | The incoming and outgoing ports for traffic flow must be specified if LAG is enabled. | ✘ |
| B | The management interface must be reassigned if LAG is disabled. | ✘ ← **เฉลยเดิม (ผิด)** |
| C | The controller must be rebooted after enabling or reconfiguring LAG. | ✔ **เฉลยใหม่ (ถูก)** |
| D | Multiple untagged interfaces on the same port must be supported. | ✘ |

**เหตุผลตามหลัก CCNA / เอกสาร Cisco WLC:**
> "When you enable LAG **or make any changes to the LAG configuration**, you must immediately reboot the controller."

* โจทย์ถามว่า *"when configuring **or removing** LAG"* → ต้องเป็นข้อกำหนดที่ครอบคลุม **ทั้งสองกรณี**
* การรีบูต = ครอบคลุมทั้งตอนเปิดและตอนถอด/แก้ LAG ✔
* ตัวเลือก B (ผูก management interface ใหม่) เป็น **ผลที่ตามมาเฉพาะตอนปิด LAG** เท่านั้น ไม่ครอบคลุมตอน configuring → ไม่ใช่คำตอบที่ดีที่สุด
* ตัวเลือก D กลับด้านกับความจริง — Cisco ระบุว่า "You **cannot** configure multiple untagged interfaces on the same port"

**สาเหตุที่เคยผิด (สำคัญ):** ในข้อสอบต้นฉบับ ข้อความ *"The controller must be rebooted…"* อยู่ที่ **ตัวอักษร B** แต่ในไฟล์นี้ตัวเลือกถูกเรียงใหม่ ข้อความเดียวกันย้ายไปอยู่ที่ **ตัวอักษร C**
→ รอบตรวจก่อนหน้า (5 ส.ค.) ลอก "ตัวอักษร B" มาโดยไม่ได้เทียบ "ข้อความ" จึงกลายเป็นเฉลยผิด
→ รอบนี้แก้เป็น **C** พร้อมเขียนคำอธิบายใหม่ให้ตรงกับเฉลย และใส่หมายเหตุอธิบายสาเหตุไว้ในตัวคำอธิบายแล้ว

---

## 2) 🟡 โจทย์ต้นฉบับบกพร่อง — เฉลยคงเดิม แต่ควรรู้ก่อนเข้าห้องสอบ

ข้อเหล่านี้ **ไม่ได้แก้** เพราะเฉลยตรงกับที่ข้อสอบจริงยึด แต่ตัวโจทย์เองมีจุดอ่อน ควรรู้ไว้เพื่อไม่สับสนตอนฝึก

| ข้อ | ปัญหาของโจทย์ต้นฉบับ | เฉลยที่ยึด |
|----:|:---|:---:|
| 159 | ถามว่า "data plane ทำอะไร" แต่มี **สองตัวเลือกที่เป็น data plane ทั้งคู่** — B (forwards traffic to the next hop) และ C (looks up an egress interface in the FIB) | **B** (ตรงกับ V1 ข้อ 442) |
| 350 | ถาม "Choose two" แต่ตามหลักจริงเราเตอร์จะติดตั้ง **3 เส้นทาง** (OSPF /30, OSPF /16, EIGRP /32) เพราะเป็นคนละพรีฟิกซ์ | **A, D** |
| 602 | ทั้ง A (AD 115) และ D (AD 100) เป็น host route /32 ไปยัง R3 เหมือนกัน — **ทำงานได้ทั้งคู่** เพราะ /32 ชนะด้วย longest prefix match ไม่เกี่ยวกับ AD | **A** |
| 37 | ทุกตัวเลือกใช้ `192.168.0.0` ซึ่งเป็น network address นำไปตั้งบนอินเทอร์เฟซไม่ได้จริง — ข้อนี้ตัดสินที่ **ซับเน็ตมาสก์** อย่างเดียว (8 ชั้น × 30–40 users → ต้อง /23) | **B** |
| 747 | ทั้ง A (Creates the IP routing table) และ C (Makes a routing decision) ตีความเป็น control plane ได้ทั้งคู่ | **C** (สอดคล้องกับข้อ 794 ในชุดเดียวกัน) |
| 506 | ภาพ exhibit สแกนไม่ชัดตรงบรรทัด `S 10.1.2.112/28` (อาจอ่านเป็น 10.1.12.112) — **ยืนยันแล้วจากภาพชุด V1 ข้อ 545 ที่คมกว่า ว่าเป็น 10.1.2.112/28** | **C** |

---

## 3) วิธีตรวจรอบนี้ (ทำใหม่ทั้งหมด ไม่ได้อ่านจากรายงานเก่า)

1. **ดึงข้อมูลจริงจากไฟล์ด้วยสคริปต์** → ได้ 808 ข้อครบ (682 เลือกตอบ + 126 drag)
2. **ตรวจโครงสร้างอัตโนมัติ** — id ซ้ำ, เฉลยชี้ไปตัวเลือกที่ไม่มีอยู่, "Choose two" แต่เฉลยตัวเดียว, `kind` ไม่ตรงกับโจทย์, ตัวเลือกข้อความซ้ำกัน, drag ที่หมวดว่าง/ไอเทมซ้ำ
   → **พบปัญหา 0 รายการ**
3. **ไล่อ่านโจทย์ + ตัวเลือก + เฉลย ทีละข้อครบทั้ง 808 ข้อ** เทียบกับหลัก CCNA 200-301
4. **สคริปต์ตรวจความขัดแย้งข้ามชุด** — โจทย์เดียวกันใน V1 (1,241 ข้อ) / V2 / V3 (317 ข้อ) ต้องเฉลย "ข้อความ" ตรงกัน
   → พบ 37 กลุ่มที่ตัวอักษรต่างกัน ตรวจทีละกลุ่มแล้ว **ทั้งหมดเป็นแค่การสลับลำดับตัวเลือก/พิมพ์ต่างกันเล็กน้อย ไม่ใช่เฉลยขัดแย้งกันจริง**
5. **เปิดภาพ exhibit จริงเพื่อคำนวณเองใหม่** สำหรับข้อที่เฉลยขึ้นกับภาพล้วน ๆ

### ภาพ exhibit ที่เปิดคำนวณเองแล้วยืนยันว่าเฉลยถูก

| ข้อ | สิ่งที่ตรวจจากภาพ | ผล |
|----:|:---|:---:|
| 75 / 123 | โจทย์เกือบเหมือนกัน แต่ภาพต่างกัน: **#75** ทั้งสองฝั่งเป็น SFP-LR แต่ SiteA มี `reliability 166/255` → interface errors • **#123** SiteA เป็น **SFP-SR** ส่วน SiteB เป็น **SFP-LR** บนไฟเบอร์ single-mode 7 กม. → ผิดชนิด transceiver | ✅ A / D ถูกทั้งคู่ |
| 90 | ตาราง: static 3 เส้นไป 10.10.13.0/25 ด้วย AD 111, 112, **108** → AD ต่ำสุด 108 via 10.10.10.10 | ✅ B |
| 135 | 3,612,699 input errors = CRC ทั้งหมด, full duplex, 0 collisions → สายเสีย | ✅ C |
| 156 | 923 / 225 / 3641 hosts + growth 10% → ต้อง /22, /24, /20 | ✅ C |
| 173 | Zone 3 กับ Zone 4 ใช้ช่องสัญญาณเดียวกันและซ้อนทับกัน | ✅ C |
| 184 | S2 (prio 30000, MAC BB:…) ชนะ S3 (prio 30000, MAC CC:…) | ✅ B |
| 274 | ต้องทำ trunk ผ่านเส้น SW4→SW11→SW9 โดยอนุญาต VLAN 14 **และ 108** | ✅ B |
| 310 | SW1 f0/0 เป็น `switchport access vlan 2` → NewSW ต้อง trunk + **native vlan 2** + allowed 2,10 | ✅ D |
| 372 | R1 Hello 10/Dead 40 ↔ R2 Hello 20/Dead 80 → ตัวแปรเดียวที่ต่างคือ timer | ✅ A |
| 505 | 10.0.1.15 ตกใน `10.0.1.0/28 [90/10] via 192.168.0.7` (ยาวสุด) | ✅ A |
| 506 | 10.1.2.126 ตกใน `S 10.1.2.112/28` (.112–.127) | ✅ C |
| 552 | มี host route `10.0.1.5/32 via 10.0.1.50` ตรงตัว | ✅ D |
| 568 | 10.56.192.1 ไม่อยู่ทั้ง /17 (0–127) และ /18 (128–191) → ตกไป default | ✅ A |
| 584 | ต้องเป็นเส้นออก **g0/1 (ฝั่ง R2)** + AD 6 > 1 จึงเป็น floating | ✅ D |
| 585 | EIGRP (AD 90) ชนะ OSPF (110) และมี 3 เส้น metric เท่ากัน → load balance F0/10-12 | ✅ A,B,C |
| 606 | 10.56.0.62 ตกใน /26 (Vlan58) ซึ่งยาวกว่า /24, /17, /16 | ✅ A |
| 688 / 707 | โจทย์เหมือนกันแต่คนละ config: **#688** ไม่มี `ip arp inspection trust` → พอร์ตเป็น untrusted • **#707** `ip arp inspection vlan 5-10` → ตัดทิ้ง ARP ที่ binding ไม่ถูกต้อง | ✅ C / A ถูกทั้งคู่ |
| 751 | ใน exhibit มี username, domain-name, RSA key ครบแล้ว แต่ **ไม่มี enable secret** และ vty ยังเป็น `transport input telnet` | ✅ A,C |
| 517 / 807 | นับ JSON: #517 = 4 keys • #807 = 1 object, 3 keys, 3 arrays | ✅ D / C |

---

## 4) ผลพลอยได้ — พบ Version 1 ผิด 1 ข้อ (ยังไม่ได้แก้)

จากการเทียบข้ามชุด พบว่า **V1 ข้อ 78** เฉลยผิด:

> *Several new coverage cells are required to improve the Wi-Fi network of an organization. Which two standard designs are recommended? (Choose two.)*

V1 ตอบ **B, E** โดย E คือ *"For maximum throughput, the WLC is configured to dynamically set adjacent access points to the same channel"* ← **ผิดตามหลัก CCNA** เพราะการตั้ง AP ที่อยู่ติดกันให้ใช้ช่องเดียวกันทำให้เกิด **co-channel interference**

เทียบกับ **V2 ข้อ 25** ที่ตอบ **A, C** (5 GHz มีช่อง non-overlapping มากกว่า + เซลล์ที่ซ้อนกันต้องใช้ช่องที่ไม่ทับกัน) ซึ่ง **ถูกต้อง**

> 📌 อยู่นอกขอบเขตงานรอบนี้ (ตรวจ V2) — แจ้งไว้เพื่อพิจารณาแก้ในไฟล์ `questions.ts` ต่อไป

---

## 5) สิ่งที่แก้ในไฟล์

| ไฟล์ | การเปลี่ยนแปลง |
|:---|:---|
| `src/data/questionsV2.ts` | ข้อ 283: `correct: ['B']` → `correct: ['C']` + เขียนคำอธิบายใหม่ให้เชียร์ C พร้อมหมายเหตุสาเหตุที่เคยผิด |

* โจทย์ ตัวเลือก และ id **ไม่ถูกแตะต้อง** ทั้ง 808 ข้อ
* `npx tsc -b` ผ่าน (exit 0)
* รันสคริปต์ตรวจโครงสร้างซ้ำหลังแก้ → 808 ข้อครบ, ปัญหาเชิงโครงสร้าง 0 รายการ

---

## 6) สรุปสั้นสำหรับใช้ฝึกทำ

✅ **Version 2 ทั้ง 808 ข้อ ใช้ฝึกได้แล้ว** — เฉลยถูกต้องตามหลัก CCNA 200-301 ครบทุกข้อ
🔴 มีเพียง **ข้อ 283** ข้อเดียวที่เฉลยผิดและแก้เรียบร้อยแล้ว (B → **C**)
🟡 ตอนเจอ **ข้อ 37, 159, 350, 506, 602, 747** ให้รู้ไว้ว่าโจทย์ต้นฉบับมีจุดบกพร่อง — จำเฉลยที่ข้อสอบยึดไว้ อย่าเสียเวลาถกเถียงในห้องสอบ
